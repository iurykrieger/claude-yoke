#!/bin/bash
# skills-format.test.sh — verifies every SKILL.md has valid frontmatter
# (name + description present, well-formed YAML).
# Real logic lands in Sprint 8 (CI workflow gating future PRs).

exit 0
