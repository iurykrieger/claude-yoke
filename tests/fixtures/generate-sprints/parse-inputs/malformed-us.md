# Acceptance Criteria — malformed fixture

> Generated by /yoke:acceptance-criteria fixture on 2026-05-03.
> PRD: tests/fixtures/generate-sprints/parse-inputs/malformed-us.md (self).
> Tech Spec: not applicable (fixture).
> Status: ratified
> Ratified by: fixture
> Ratified at: 2026-05-03T00:00:00Z

Used by `tests/acceptance/2026-05-03-generate-sprints-skill/us-003-parse-inputs-shape.test.sh`.

The first block is well-formed. The second block omits the
`#### Definition of Done` sub-section, which is a binding shape
requirement of every US block per `templates/acceptance-criteria.md`.
The parser MUST detect this and exit non-zero with a `wm:`-prefixed
stderr line.

## User Stories

### US-001 — Well-formed user story

As a Yoke user, I want a well-formed first block, so the malformation
appears in isolation in US-002.

#### Definition of Done

- [ ] Parses cleanly on its own.

#### Acceptance Criteria

- **AC-001-1:** Parser sees one valid block before encountering the
  malformation.

### US-002 — Malformed user story (missing Definition of Done)

As a Yoke user, I want this block to lack the DoD sub-heading, so the
parser surfaces a malformation diagnostic.

#### Acceptance Criteria

- **AC-002-1:** Parser exits non-zero with `wm:` stderr.

## Functional Requirements

- **FR-1:** Malformation is detected at the document level, not after
  emitting partial JSON.

## Sensor pool

- tests-smoke
