#!/bin/bash
# plugin-install.test.sh — verifies the plugin installs from a clean
# Claude Code environment via /plugin install.
# Real logic lands in Sprint 8 (CI workflow gating future PRs).

exit 0
