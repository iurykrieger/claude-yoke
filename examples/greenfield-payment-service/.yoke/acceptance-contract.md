# Acceptance Contract — Payment reversal service (v0)

> Generated by `/yoke:acceptance-contract` from approved PRD + Tech Spec on 2026-04-25.
> PRD: `.yoke/prd.md`
> Tech Spec: `.yoke/tech-spec.md`
> Status: ratified
> Ratified by: example-user
> Ratified at: 2026-04-25T13:45:00Z

> **Binding statement (Trigger 3).** Approving this contract operationally
> defines "done" for this task as "passes every criterion below". Changes
> during runtime require a fresh ratification round. Sprint contracts
> negotiated by Implementation and Validation Agents at runtime can refine
> interpretation **inside** this envelope but cannot relax it. A sprint
> contract attempting to modify the Contract escalates via Trigger 4.

## Use cases (BDD scenarios)

### Scenario 1 — POST /reversals on a settled transaction (Tech-Spec task 1.1)
Given a transaction `tx_abc` with state `settled` and `amount: 1000 USD`
When the support tool POSTs `{tx_id: "tx_abc", requestor_id: "support-42"}` to `/reversals`
Then the response is HTTP 201 with body `{tx_id: "tx_abc", refund_id: <string>, refund_amount: 1000, currency: "USD"}`
Fixture: `tests/fixtures/scenario-1-settled-tx.json`
Sensors: [unit, contract]

### Scenario 2 — Gateway client makes exactly one POST (Tech-Spec task 1.2)
Given a settled transaction `tx_abc`
When the support tool requests reversal
Then the service issues exactly one `POST https://gateway.test/v1/refunds`
Fixture: `tests/fixtures/scenario-2-gateway-mock.yaml`
Sensors: [unit, contract]

### Scenario 3 — Audit event emitted on success (Tech-Spec task 1.3)
Given a settled transaction reversed successfully
When the reversal completes
Then exactly one `reversal.completed` event is published to `payments-events` containing tx_id, requestor_id, ISO-8601 timestamp, and refund_amount
Fixture: `tests/fixtures/scenario-3-event-shape.json`
Sensors: [unit, contract]

### Scenario 4 — Concurrent reversal requests (Tech-Spec task 2.1)
Given a settled transaction `tx_abc`
When 50 concurrent reversal requests arrive
Then exactly one `reversal.completed` event is emitted; exactly one gateway POST is made; the other 49 responses are HTTP 409 with body `{"reason": "already_reversed"}`
Fixture: `tests/fixtures/scenario-4-stress.yaml`
Sensors: [unit, contract, structural]

### Scenario 5 — Gateway 429 retries (Tech-Spec task 2.2)
Given the gateway returns HTTP 429 four times then HTTP 200
When the service requests a reversal
Then the service retries with exponential backoff and the reversal completes successfully
Fixture: `tests/fixtures/scenario-5-rate-limit.yaml`
Sensors: [unit, contract]

### Scenario 6 — Audit-event PII redaction (Tech-Spec task 3.1)
Given a customer transaction with email and phone in the source PII
When a reversal completes
Then the emitted `reversal.completed` event contains `email_hash` and `phone_hash` (SHA-256 strings) and does NOT contain raw `email` or raw `phone`
Fixture: `tests/fixtures/scenario-6-pii-redaction.json`
Sensors: [unit, contract, structural]

### Scenario 7 — Reject non-settled transaction (Tech-Spec task 3.2)
Given a transaction in state `pending` or `failed`
When the support tool requests reversal
Then the response is HTTP 422 with body `{"reason": "tx_not_settled"}`; a `reversal.failed` event is emitted; no gateway call is made
Fixture: `tests/fixtures/scenario-7-not-settled.yaml`
Sensors: [unit, contract]

## Functional requirements

- [ ] **FR-1** — Reversal is idempotent on `tx_id` (PRD invariant). Sensor: `npm test -- idempotency`.
- [ ] **FR-2** — Refund amount equals original tx.amount (PRD invariant). Sensor: `npm test -- refund-amount`.
- [ ] **FR-3** — Audit events emitted for every reversal (PRD invariant). Sensor: `npm run test:contract -- events`.
- [ ] **FR-4** — Audit events redact PII (LGPD art. 46). Sensor: `npm run test:contract -- pii-redaction` (Scenario 6).
- [ ] **FR-5** — Gateway 429 handling with exponential backoff (PRD risk mitigation). Sensor: `npm test -- rate-limit`.
- [ ] **FR-6** — Reversal allowed only on `settled` source transactions (PRD invariant). Sensor: `npm test -- not-settled`.

## Applicable policies

Discovered via `/yoke:ask` against canonical memory:

- **PCI-DSS 3.2.1** — canonical: `policies/pci-dss-payment-data.md`. Applies to: any code path that handles card data. (Inherited from gateway client; no new exposure expected.)
- **LGPD art. 46** — canonical: `policies/lgpd-art-46.md`. Applies to: services that emit events containing customer PII. **Directly relevant** — Scenario 6 verifies enforcement.

## Sensors

### Computational

Discovered from the host project's `CLAUDE.md` (sections `## Testing`,
`## Linting`, `## Build`) plus added by the Validator during drafting.
Each bullet's first backticked segment is the runnable command.
`hooks/verify-acceptance.sh` parses these.

- linter: `npm run lint`
- type-check: `npm run lint:types`
- unit: `npm test`
- structural: `npm run test:contract`
- integration: `npm run test:integration`
- build: `npm run build`

### Inferential

Sprint-3 placeholder. Inferential sensors with full calibration metadata
ship in Sprint 5+. When added, each entry will carry:

- `prompt`: `<path>`
- `model_calibrated_against`: `<model id>`
- `calibrated_at`: `<iso8601>`
- `known_false_positives`: `<rate or count>`
- `known_false_negatives`: `<rate or count>`

For v1.0 of this example, the Acceptance Contract relies entirely on the
six computational sensors above.
