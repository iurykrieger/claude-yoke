---
task_id: <slug>-s<NN>
sprint: <N>
slug: <slug>
status: draft
created_at: <iso8601>
model: ""
traceability: ""
Migrated-from: []
---

# Sprint <NN> of <MM>: <one-line sprint name>

> Generated by `/yoke:generate-sprints` (Phase 2.5) from the approved
> `.yoke/specs/<slug>.md` and the ratified
> `.yoke/acceptance-criteria/<slug>.md`. Status flips to `approved`
> when Trigger 2.5 ratifies the sprint plan.
>
> Sprint files are runtime bundles, not sources of truth. Sources of
> truth (Acceptance Contract criteria, sensor logic) stay authoritative
> in their own files; this sprint file references them by ID and never
> inlines their bodies. Cite
> `concepts/yoke-pattern-memory-model` and the new
> `concepts/yoke-pattern-sprint-runtime-bundle` (drafted in sprint 4's
> preserve packet of the sprint-as-cycle PRD) for the load-bearing
> invariants.
>
> The frontmatter `task_id` field intentionally keeps the legacy name
> for compatibility with downstream tooling that treats every working-
> memory artifact uniformly. Its value is the sprint id (`<slug>-s<NN>`).
> The optional `Migrated-from:` array carries original `<slug>-part-N.md`
> or `<slug>-s<NN>-t<MM>.md` paths when this sprint file is the result
> of the sprint-as-cycle PRD's migration sprints.

## Sprint objective

<one paragraph capturing what this sprint delivers — the shippable
outcome a Validator can decide on at sprint end>

## Sprint DoD

<bullet list of binary, observable checks — sprint-exit conditions
the Validator evaluates to declare convergence. Each check must be a
single proposition, decidable by sensor or grep, with no ambiguity.>

- <binary check — e.g. "tests/smoke/sprint-2.test.sh exits 0">
- <binary check — e.g. "find .yoke/sprints -name '<slug>-s*.md' | wc -l == <N>">

## Tasks

<one `### Task <ID>` subsection per task, in order. Tasks remain a
concept *inside* the sprint file; they no longer carry their own
file. Each task subsection carries the four inline labels
(`**Story:**`, `**Technical implementation:**`, `**Validation:**`,
`**Acceptance criterion:**`).>

### Task <slug>-s<NN>-t01

**Story:** <one-line story — what use case this task realizes>

**Technical implementation:** <full description of how the task is
implemented: files touched, functions added, data structures,
contracts honored. Cite patterns from `concepts/yoke-pattern-*` by
name when applicable.>

**Validation:** <full description of how the task is verified: tests
added, fixtures, sensors, manual checks. This section is the input
to the Acceptance Contract's BDD scenario for this task — the Then
clauses are written from here.>

**Acceptance criterion:** <single binary, observable criterion —
pass/fail with no ambiguity>

### Task <slug>-s<NN>-t02

**Story:** <…>

**Technical implementation:** <…>

**Validation:** <…>

**Acceptance criterion:** <…>

> **US-trace clause.** When this sprint file is produced by
> `/yoke:generate-sprints` (the new flow introduced by the
> generate-sprints PRD), each Task's `**Story:**` line MUST end with a
> parenthesized `(Realizes: US-<NNN>[, US-<MMM>])` clause that names
> every User Story from the binding `.yoke/acceptance-criteria/<slug>.md`
> the task realizes. The clause is omitted only for legacy-flow sprint
> files generated by `/yoke:tech-spec` stage 3 (the legacy producer
> pre-cutover). Cite `concepts/yoke-pattern-sprint-runtime-bundle`
> Rule 1 (reference-by-ID, never inline) — the US-trace clause is the
> ID-anchor between the sprint file and the AC document; the criterion
> bodies stay in the AC.

## Functional acceptance criteria

<bullet list of criterion IDs from the binding
`acceptance-criteria/<slug>.md`. Reference-by-ID only — never inline
the criterion text. The Validator resolves each ID against the
binding contract at cycle execution.>

- <AC-criterion-id> — e.g. `wm-sprint-helpers-callable`
- <AC-criterion-id> — e.g. `templates-sprint-md-shape`

## Sensors

<bullet list of sensor IDs from `.yoke/sensors/<id>.md`. Reference-
by-ID only — never inline sensor logic. The Validator resolves each
ID and runs the sensor at cycle execution.>

- <sensor-id> — e.g. `legacy-parts-zero-residual`
- <sensor-id> — e.g. `wm-sprint-helpers-callable`

---

> Generated by `/yoke:generate-sprints` (Phase 2.5) from the approved
> `.yoke/specs/<slug>.md` and the ratified
> `.yoke/acceptance-criteria/<slug>.md`. Status flips to `approved`
> when Trigger 2.5 ratifies the sprint plan.
