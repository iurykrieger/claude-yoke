# Acceptance Criteria — <task name>

> Generated by `/yoke:acceptance-criteria` from approved PRD + Tech
> Spec on <date>.
> PRD: `.yoke/prds/<slug>.md`
> Tech Spec: `.yoke/specs/<slug>.md`
> Status: <draft | ratified>
> Ratified by: <user>
> Ratified at: <iso8601>

> **Binding statement (Trigger 3).** Approving this Acceptance Criteria
> document operationally defines "done" for this task as "passes every
> criterion below". Changes during runtime require a fresh ratification
> round. Sprint contracts negotiated by Sr Eng / Sr QA / Sr Staff at
> runtime can refine interpretation **inside** this envelope but cannot
> relax it. A sprint contract attempting to modify the Acceptance
> Criteria escalates via Trigger 4.

> **Definition of Done vs Acceptance Criteria — distinct, layered.**
> **DoD** is a single binary checklist per User Story that defines when
> the implementation is *finished*. **Acceptance Criteria** are
> multiple observable QA conditions per User Story that, given the
> implementation is done, define when the work is *acceptable* in
> quality terms. The runtime council enforces ordering: DoD checks
> must pass before any AC is evaluated for that story.

> **Sensor pool is unclassified at authoring time.** The
> `## Sensor pool` section below lists every sensor relevant to this
> task as a flat bullet list. Sr QA and Sr Staff pick which pool
> members gate which Acceptance Criterion at Phase 4 runtime, recording
> their selection in their per-cycle slice file. Authoring-time
> mandatory / complementary / optional tags are forbidden — they
> collapse a runtime-policy decision into an authoring-time decision.
> Per-sensor command, type (`computational` | `inferential`),
> `token_cost`, `time_cost`, and dispatch metadata live in
> `.yoke/sensors/<sensor-id>.md` (per `templates/sensor.md`).

## User Stories

One `### US-<NNN> — <title>` block per user story. Each block carries:

- The user-story sentence (`As a <role>, I want <capability>, so that
  <benefit>.`).
- A `#### Definition of Done` checklist of binary, completion-anchored
  items — single source of truth for "is this story finished?".
- A `#### Acceptance Criteria` list of observable quality conditions —
  each carries a stable `AC-<US>-<n>` identifier (e.g., `AC-001-1`,
  `AC-001-2`) and is phrasable as Given/When/Then or as a clean
  observable check. AC entries do **not** carry inline sensor IDs —
  sensor selection is runtime-only.

### US-001 — <short descriptive title>

As a <role>, I want <capability>, so that <benefit>.

#### Definition of Done

- [ ] <binary completion item — e.g. "Endpoint POST /api/foo accepts the documented payload">
- [ ] <binary completion item — e.g. "Unit tests under tests/unit/foo cover the happy path">
- [ ] <binary completion item — e.g. "shellcheck and the project linter exit 0 on every modified file">

#### Acceptance Criteria

- **AC-001-1:** <observable QA condition — e.g. "Endpoint returns 200 with a valid JWT in the response body when the credentials are correct">
- **AC-001-2:** <observable QA condition — e.g. "Endpoint returns 401 with `{ error: \"invalid_credentials\" }` when the password is wrong">
- **AC-001-3:** <observable QA condition — UI stories MUST include "Verified in browser" as one of the AC entries>

### US-002 — <short descriptive title>

As a <role>, I want <capability>, so that <benefit>.

#### Definition of Done

- [ ] <binary completion item>
- [ ] <binary completion item>

#### Acceptance Criteria

- **AC-002-1:** <observable QA condition>
- **AC-002-2:** <observable QA condition>

> Acceptance Criteria must be verifiable, not vague. "Works correctly"
> is rejection-grade. "Endpoint returns 200 with a valid JWT" is good.

## Functional Requirements

Cross-cutting, numbered, unambiguous. These supplement the per-US
DoD/AC with concerns that span multiple stories (regulatory,
performance, operational). Each FR is sensor-decidable.

- **FR-1:** <measurable cross-cutting requirement — e.g. "All Bash scripts authored under this PRD pass `shellcheck` with no new warnings">
- **FR-2:** <measurable cross-cutting requirement>
- **FR-3:** <measurable cross-cutting requirement>

## Sensor pool

Flat bullet list of sensor IDs relevant to this task. Each entry MUST
resolve to an existing `.yoke/sensors/<id>.md` file — the
`/yoke:acceptance-criteria` skill validates resolution before
ratification, refusing to render the Trigger 3 menu if any pool entry
fails to resolve. **No classification at authoring time.** The
runtime council picks per-criterion which pool members apply.

- <sensor-id>
- <sensor-id>
- <sensor-id>

## Open Questions

TODOs the Senior QA persona could not resolve in the grill. Block
Phase 4 when load-bearing. Use the literal word `None.` (and remove
this template list) when there are no open questions — that
suppresses the approval-menu warning.

- <question>

---

> When ready, run `/yoke:generate-sprints` to advance to Phase 2.5
> (sprint synthesis from the ratified Acceptance Criteria + Tech Spec);
> Phase 2.5 then chains into `/yoke:implement` (the agent council
> ralph loop) on the produced sprint files.
