#!/bin/bash
# run-sensors.sh — execute discovered sensors and emit structured output.
# Real logic lands in Sprint 3 (shell-command sensors only); richer sensor
# types (structural fixtures, inferential semantic judges with rubrics) ship
# in later sprints.

exit 0
