# Acceptance Contract — Phase 3 acceptance-criteria refactor

> Generated by `/yoke:acceptance-contract` from approved PRD + Spec
> + per-sprint runtime bundles on 2026-05-03.
> PRD: `.yoke/prds/2026-05-03-acceptance-criteria-refactor.md`
> Spec: `.yoke/specs/2026-05-03-acceptance-criteria-refactor.md`
> Sprints: `.yoke/sprints/2026-05-03-acceptance-criteria-refactor-s01.md` … `-s05.md`
> (one BDD scenario per `### Task <ID>` anchor — 21 total)
> Status: ratified
> Ratified by: iurykrieger
> Ratified at: 2026-05-03T07:20:26Z

> **Binding statement (Trigger 3).** Approving this contract operationally
> defines "done" for this task as "passes every criterion below". Changes
> during runtime require a fresh ratification round. Sprint contracts
> negotiated by Implementation and Validation Agents at runtime can refine
> interpretation **inside** this envelope but cannot relax it. A sprint
> contract attempting to modify the Contract escalates via Trigger 4.

> **Sensor binding lives here, not inline.** Each criterion below carries
> a `### Validation` sub-section listing the sensors that gate it and the
> interpretation guidance for each. The per-sensor command, type
> (`computational` | `inferential`), `token_cost`, `time_cost`, and the
> `command:`-or-`agent:` dispatch metadata live in
> `.yoke/sensors/<sensor-id>.md` (per `templates/sensor.md`). Adding a
> sensor to a contract is a contract-only change; removing one does not
> affect the sensor file.

> **Self-referential note.** This task refactors the Phase 3 artifact
> itself. The contract is authored under the legacy
> `acceptance-contract` shape and lives at
> `.yoke/acceptance-contracts/2026-05-03-acceptance-criteria-refactor.md`;
> after Sprint 1 ships, the directory rename means subsequent reads of
> this artifact go through `wm_acceptance_criteria_path` only if the
> file is also relocated. The task does NOT migrate this file (per the
> PRD's no-historical-migration policy); the file remains as a frozen
> historical record of the v3.x → v4.0.0 cutover binding.

## Use cases (BDD scenarios)

**Exactly one scenario per task anchor — 21 total across 5 sprints.**
Each scenario carries the 1:1 `Task: <task-id>` anchor referencing the
sprint file's `### Task <ID>` subsection. Then clauses are drafted
from each task's `**Validation:**` inline label; binary observable
checks come from each task's `**Acceptance criterion:**`.

### Scenario 1 — wm_acceptance_criteria_path callable; legacy helper deleted
Task: 2026-05-03-acceptance-criteria-refactor-s01-t01
Given the v3.x repo with `wm_acceptance_contract_path` defined in `lib/working-memory/paths.sh`
When I edit `lib/working-memory/paths.sh` to add `wm_acceptance_criteria_path` and delete `wm_acceptance_contract_path` (no delegation, no shim)
Then `bash tests/unit/working-memory-paths.test.sh` exits 0 AND `grep -qE '^wm_acceptance_contract_path\(\)' lib/working-memory/paths.sh` exits non-zero AND `bash -c 'source lib/working-memory/paths.sh; type wm_acceptance_contract_path 2>&1'` returns non-zero with `not found`
Fixture: none
Sensors: [tests-runtime, lint]

### Scenario 2 — Template renamed verbatim with H1 retitled
Task: 2026-05-03-acceptance-criteria-refactor-s01-t02
Given `templates/acceptance-contract.md` exists at the legacy path
When I `git mv templates/acceptance-contract.md templates/acceptance-criteria.md` and edit only the H1 line to `# Acceptance Criteria — <task name>`
Then `bash tests/unit/template-files.test.sh` exits 0 AND `test -f templates/acceptance-criteria.md` AND `test ! -f templates/acceptance-contract.md` AND `git log --diff-filter=R --follow templates/acceptance-criteria.md | head -1` returns a non-empty commit line
Fixture: none
Sensors: [tests-runtime, lint]

### Scenario 3 — Skill directory renamed; verb routes to new path
Task: 2026-05-03-acceptance-criteria-refactor-s01-t03
Given `skills/acceptance-contract/SKILL.md` exists at the legacy path
When I `git mv skills/acceptance-contract skills/acceptance-criteria` and edit the SKILL.md H1 to `# /yoke:acceptance-criteria`, plus update `.claude-plugin/plugin.json` and `marketplace.json` skill paths
Then `bash tests/unit/skill-files.test.sh` exits 0 AND `test -f skills/acceptance-criteria/SKILL.md` AND `test ! -d skills/acceptance-contract` AND `find skills -type d -name 'acceptance-contract*'` returns empty
Fixture: none
Sensors: [tests-runtime, lint]

### Scenario 4 — Plugin version bumped to 4.0.0
Task: 2026-05-03-acceptance-criteria-refactor-s01-t04
Given `.claude-plugin/plugin.json` carries `"version": "3.0.0"` and a `description` field naming "v3.0 agent council protocol"
When I set `"version": "4.0.0"` and update the description's leading version reference to mention "v4.0 acceptance-criteria QA cutover" while preserving the rest
Then `bash tests/unit/plugin-manifest.test.sh` exits 0 AND `jq -r .version .claude-plugin/plugin.json` returns `4.0.0` AND `jq -r .description .claude-plugin/plugin.json` contains the literal substring `v4.0` AND `bash tests/smoke/working-memory-migration.test.sh` exits 0 (no upstream regression)
Fixture: none
Sensors: [tests-runtime, tests-smoke, lint]

### Scenario 5 — Acceptance-criteria template body in new hierarchy
Task: 2026-05-03-acceptance-criteria-refactor-s02-t01
Given `templates/acceptance-criteria.md` carries the legacy template body (post Sprint 1 rename)
When I rewrite the body to render the new hierarchy: header → binding statement → `## User Stories` → per-story (`### US-### — <title>` + `#### Definition of Done` + `#### Acceptance Criteria`) → `## Functional Requirements` → `## Sensor pool` → `## Open Questions`
Then `bash tests/unit/templates-shape.test.sh` exits 0 AND the H2/H3/H4 heading order matches the documented sequence AND at least one `### US-` block contains both `#### Definition of Done` and `#### Acceptance Criteria` AND the H1 line matches `^# Acceptance Criteria — `
Fixture: none
Sensors: [tests-runtime, lint]

### Scenario 6 — PRD template stripped of User Stories block
Task: 2026-05-03-acceptance-criteria-refactor-s02-t02
Given `templates/prd.md` carries a `## User Stories` block with example US-001 / US-002 sub-sections
When I delete the `## User Stories` block (heading + body + sub-sections) and audit neighbouring sections to point at `/yoke:acceptance-criteria` as the US owner, while preserving `## Functional Requirements`
Then `bash tests/unit/prd-template-shape.test.sh` exits 0 AND `grep -E '^## User Stories$' templates/prd.md` returns nothing AND `grep -E '^## Functional Requirements$' templates/prd.md` returns exactly 1 AND no orphan `### US-` heading remains
Fixture: none
Sensors: [tests-runtime, lint]

### Scenario 7 — /yoke:discover skill drops US-### enumeration guidance
Task: 2026-05-03-acceptance-criteria-refactor-s02-t03
Given `skills/discover/SKILL.md` instructs the dialogue to enumerate `US-001`, `US-002`, etc., and asks "What are the 3-6 user stories that make up v0?"
When I remove all US-### enumeration guidance and add a closing line in Round 3 pointing at `/yoke:acceptance-criteria` as the US owner, preserving FR-N guidance
Then `bash tests/unit/skill-discover-shape.test.sh` exits 0 AND `grep -i 'us-00[0-9]' skills/discover/SKILL.md` returns 0 matches AND `grep -F '/yoke:acceptance-criteria' skills/discover/SKILL.md` returns at least 1 match
Fixture: none
Sensors: [tests-runtime, lint]

### Scenario 8 — Sensor-pool resolution helper exits non-zero on unresolved IDs
Task: 2026-05-03-acceptance-criteria-refactor-s02-t04
Given `lib/sensors/resolve-pool.sh` does not yet exist
When I create the script with a `resolve_sensor_pool <artifact-path>` function that mapfile-extracts the `## Sensor pool` block, parses one ID per `^- ` bullet, and asserts each resolves to `.yoke/sensors/<id>.md`
Then `bash tests/unit/sensor-pool-resolve.test.sh` exits 0 across three fixtures (all-valid → exit 0; one-unknown → non-zero with `wm: sensor pool unresolved: <id>` on stderr; empty → exit 0)
Fixture: `tests/unit/fixtures/sensor-pool/`
Sensors: [tests-runtime, lint]

### Scenario 9 — Skill body documents pre-flight, resume, and grill
Task: 2026-05-03-acceptance-criteria-refactor-s03-t01
Given `skills/acceptance-criteria/SKILL.md` carries the legacy skill body (post Sprint 1 rename)
When I rewrite the body to document the Senior QA persona, pre-flight checks (config, active task, PRD/Spec approved), a ≤ 25-line PRD+Tech-Spec resume, a 1–5 round lettered-options grill across five buckets (US enumeration, DoD per US, AC per US, sensor pool, FRs), challenge-vague-answers rule, Quick Round / Full Flow split, materialization step, sensor-pool validation, and Trigger 3 menu rendering
Then `bash tests/unit/skill-acceptance-criteria-shape.test.sh` exits 0 AND the H1 matches `^# /yoke:acceptance-criteria` AND the documented sections appear in declared order AND lettered-options examples are present AND `next_skill = /yoke:implement` appears verbatim AND the binding-statement paragraph contains "operationally defines 'done'"
Fixture: none
Sensors: [tests-runtime, lint, code-review]

### Scenario 10 — Skill materializes artifact with binding metadata header
Task: 2026-05-03-acceptance-criteria-refactor-s03-t02
Given a fixture task with PRD and Tech Spec marked approved under a temp `.yoke/`
When I drive the skill with canned grill answers via stdin and select option 1 in the Trigger 3 menu
Then `bash tests/smoke/acceptance-criteria-skill.test.sh` exits 0 AND `.yoke/acceptance-criteria/<slug>.md` exists with `Status: ratified`, `Ratified by`, and `Ratified at` populated AND at least one `### US-` block contains both `#### Definition of Done` and `#### Acceptance Criteria` AND the binding-statement paragraph appears above `## User Stories`
Fixture: `tests/smoke/fixtures/acceptance-criteria-skill/happy-path/`
Sensors: [tests-smoke, lint, code-review]

### Scenario 11 — Skill refuses ratification when sensor pool has unresolved IDs
Task: 2026-05-03-acceptance-criteria-refactor-s03-t03
Given a fixture grill output that includes the sensor ID `phantom-sensor` in the pool
When the skill reaches step 7 (sensor-pool validation) and invokes `resolve_sensor_pool` against the materialized artifact
Then the skill exits non-zero AND stderr contains `wm: sensor pool unresolved: phantom-sensor` AND the artifact file is left in `Status: draft` (no ratified flip)
Fixture: `tests/smoke/fixtures/acceptance-criteria-skill/unknown-sensor/`
Sensors: [tests-smoke, lint]

### Scenario 12 — Quick-Round fast-track converges in fewer turns than Full Flow
Task: 2026-05-03-acceptance-criteria-refactor-s03-t04
Given two fixture variants: a Quick-Round-eligible task (PRD goals 1:1 to candidate US, sprint DoDs map cleanly to US-DoD) and a Full-Flow task (ambiguous goals)
When I drive the skill against each variant with appropriate canned answers
Then the Quick-Round variant produces a valid artifact AND its dialogue-turn count is strictly less than the Full-Flow variant's count AND `bash tests/unit/skill-acceptance-criteria-shape.test.sh` asserts the Quick-Round section lists three Quick-Round options
Fixture: `tests/smoke/fixtures/acceptance-criteria-skill/quick-round/`, `tests/smoke/fixtures/acceptance-criteria-skill/full-flow/`
Sensors: [tests-smoke, tests-runtime, lint]

### Scenario 13 — Sr QA persona prompt reads new artifact shape with sensor selection
Task: 2026-05-03-acceptance-criteria-refactor-s04-t01
Given `agents/sr-qa.md` references the legacy artifact shape (BDD scenarios, flat sensor list)
When I rewrite artifact-shape references to `### US-` blocks with `#### Definition of Done` / `#### Acceptance Criteria`, the document-level `## Sensor pool`, and add a `## Sensor selection at runtime` section instructing the persona to pick pool sensors per AC and write selection rationale to its slice file under `## Sensor selection`
Then `bash tests/runtime/sr-qa-prompt-shape.test.sh` exits 0 AND frontmatter `name:` and `tools:` keys are intact AND the `## Sensor selection at runtime` H2 exists AND the per-AC instruction matches `for each AC.*pool sensors` AND `acceptance-contract` does not appear in the body
Fixture: none
Sensors: [tests-runtime, code-review, lint]

### Scenario 14 — Sr Staff persona prompt updated for inferential pool selection
Task: 2026-05-03-acceptance-criteria-refactor-s04-t02
Given `agents/sr-staff.md` references the legacy artifact shape and lists inferential sensors inline
When I update artifact-shape references analogously to s04-t01 and add a `## Sensor selection at runtime` section scoped to inferential pool sensors (`llm-as-judge`, `code-review`), preserving the `review-skill: /review` frontmatter and the `/ultrareview` anti-scope
Then `bash tests/runtime/sr-staff-prompt-shape.test.sh` exits 0 AND frontmatter `review-skill: /review` is intact AND the `## Sensor selection at runtime` H2 exists AND `/ultrareview` anti-scope sentence is preserved AND `acceptance-contract` does not appear
Fixture: none
Sensors: [tests-runtime, code-review, lint]

### Scenario 15 — lib/runtime resolves new path and emits AC tuples
Task: 2026-05-03-acceptance-criteria-refactor-s04-t03
Given `lib/runtime/cycle.sh` and `lib/runtime/council.sh` carry `wm_acceptance_contract_path` calls and inline `.yoke/acceptance-contracts/` literals
When I replace every legacy call with `wm_acceptance_criteria_path` and create `lib/runtime/criteria-parse.sh` exporting `criteria_parse <artifact-path>` that emits `<US-ID>|<AC-ID>` tuples one per line
Then `bash tests/runtime/cycle-resolves-criteria-path.test.sh` exits 0 AND `grep -E 'acceptance-criteria' lib/runtime/cycle.sh` returns at least 1 match AND `grep -E 'acceptance-contracts' lib/runtime/cycle.sh` returns 0 matches AND `criteria_parse tests/runtime/fixtures/criteria-three-us.md` emits the expected tuples in order AND `criteria_parse tests/runtime/fixtures/criteria-malformed.md` exits non-zero
Fixture: `tests/runtime/fixtures/criteria-three-us.md`, `tests/runtime/fixtures/criteria-malformed.md`
Sensors: [tests-runtime, lint]

### Scenario 16 — Arbiter classifies AC-disagreements as direct contradictions
Task: 2026-05-03-acceptance-criteria-refactor-s04-t04
Given the four pre-existing arbiter branch fixtures (consensus, direct-contradiction, importance-disagreement, tone-only) and a new `ac-disagreement.cycle/` fixture where Sr QA marks `AC-002-3` as failing and Sr Staff marks it as passing
When I extend `agents/council-arbiter.md` to enumerate the new artifact hierarchy and classify (a) DoD disagreements as direct contradictions, (b) AC-level disagreements as direct contradictions, (c) sensor-selection disagreements as importance disagreements, while preserving the JSON verdict schema
Then `bash tests/sensors/council-arbiter.test.sh` exits 0 across all five branch fixtures AND on the AC-disagreement cycle the JSON parses to `consensus: false` with exactly one entry in `contradictions[]` referencing `AC-002-3` and the `sr-qa × sr-staff` pair
Fixture: `tests/runtime/fixtures/arbiter/ac-disagreement.cycle/`
Sensors: [tests-runtime, tests-sensors, code-review, lint]

### Scenario 17 — Project CLAUDE.md updated with v4.0.0 migration history entry
Task: 2026-05-03-acceptance-criteria-refactor-s05-t01
Given `CLAUDE.md` (this repo) references `.yoke/acceptance-contracts/`, `/yoke:acceptance-contract`, and the legacy Phase 3 artifact name throughout
When I update the directory table, Six-phase flow, Non-negotiable invariants, and Sprint discipline sections to use the new name, append a v4.0.0 entry to "Migration history" dated 2026-05-03, and add the DoD-vs-AC distinction note
Then `bash tests/unit/claude-md-shape.test.sh` exits 0 AND `grep -E 'acceptance-criteria' CLAUDE.md | wc -l` returns at least 5 AND `acceptance-contracts/` matches outside the "Migration history" block return 0 AND the v4.0.0 entry exists with date 2026-05-03 AND Phase 3 in the Six-phase flow block names `acceptance-criteria.md`
Fixture: none
Sensors: [tests-runtime, lint]

### Scenario 18 — Host CLAUDE.md template updated for downstream bootstraps
Task: 2026-05-03-acceptance-criteria-refactor-s05-t02
Given `templates/project-claude-md.md` references the legacy artifact name in its body
When I update artifact references to `acceptance-criteria`, the Phase 3 description to `acceptance-criteria.md`, and add a brief DoD-vs-AC note in "Where things live" or "Sprint discipline"
Then `bash tests/unit/host-claude-md-template-shape.test.sh` exits 0 AND `grep -E 'acceptance-criteria' templates/project-claude-md.md | wc -l` returns at least 3 AND `grep -E 'acceptance-contract' templates/project-claude-md.md | wc -l` returns 0 AND the `/yoke:acceptance-criteria` skill verb appears at least once
Fixture: none
Sensors: [tests-runtime, lint]

### Scenario 19 — docs/architecture.md reflects new artifact and sensor pool semantics
Task: 2026-05-03-acceptance-criteria-refactor-s05-t03
Given `docs/architecture.md` describes Phase 3 in the legacy artifact shape (BDD scenarios with flat sensor list)
When I update the Phase 3 prose to describe User Stories → DoD → AC → Sensor pool, the diagram description to name `.yoke/acceptance-criteria/`, and the sensor-binding paragraph to document runtime selection by Sr QA and Sr Staff
Then `bash tests/unit/architecture-md-shape.test.sh` exits 0 AND `grep -E 'acceptance-criteria' docs/architecture.md | wc -l` returns at least 3 AND the document mentions "Definition of Done" and "Acceptance Criteria" AND a runtime-selection narrative is present (regex `runtime selection|select.*pool`) AND `acceptance-contracts/` matches outside intentional historical refs return 0
Fixture: none
Sensors: [tests-runtime, lint]

### Scenario 20 — Migration v3-to-v4 doc carries seven required sections
Task: 2026-05-03-acceptance-criteria-refactor-s05-t04
Given `docs/migration-v3-to-v4.md` does not exist
When I create the doc with sections: `## What changed`, `## Renames`, `## New artifact shape`, `## Sensor pool — no classification at authoring time`, `## Behavioral changes`, `## What is NOT migrated`, `## Upgrade steps`, including a rename table covering directory, skill verb, template, and helper renames
Then `bash tests/unit/migration-doc-shape.test.sh` exits 0 AND `test -f docs/migration-v3-to-v4.md` AND the seven required H2 headings exist verbatim AND the rename table contains all four documented rename rows
Fixture: none
Sensors: [tests-runtime, lint]

### Scenario 21 — Final orphan-ref smoke sensor returns zero unaccounted matches
Task: 2026-05-03-acceptance-criteria-refactor-s05-t05
Given the cutover is complete (Sprints 1-4 + s05-t01..t04 merged); some references to `acceptance-contract` remain in `docs/migration-v3-to-v4.md` and the "Migration history" block of `CLAUDE.md`
When I add `tests/smoke/orphan-acceptance-contract-refs.test.sh` that greps recursively over active code paths with an allowlist for the migration doc and the Migration-history block, plus a self-test fixture under `tests/smoke/fixtures/orphan-ref-positive/`
Then `bash tests/smoke/orphan-acceptance-contract-refs.test.sh` against the live repo exits 0 AND running the same script with `--fixture tests/smoke/fixtures/orphan-ref-positive/` exits non-zero (self-test of the negative case)
Fixture: `tests/smoke/fixtures/orphan-ref-positive/`
Sensors: [tests-smoke, lint]

## Functional requirements

Measurable, observable. Each FR maps to a concrete sensor or fixture.
Each criterion carries a `### Validation` sub-section enumerating the
sensors that gate it and how to interpret each sensor's feedback.

### Criterion FR-1 — Path helper rename + legacy-helper deletion are atomic in lib/working-memory/paths.sh

After Sprint 1, `wm_acceptance_criteria_path <slug>` is the single
canonical helper for the artifact path, and `wm_acceptance_contract_path`
is removed entirely (no delegation, no shim, no deprecation warning).

### Validation

- **tests-runtime** — pass = `bash tests/unit/working-memory-paths.test.sh` exits 0 and the test asserts both helper presence (new) and helper absence (legacy); fail = either assertion fails or the script errors before assertions run.
- **lint** — pass = `shellcheck lib/working-memory/paths.sh` exits 0 with no new warnings; fail = any new warning attributable to this PRD's edits.

### Criterion FR-2 — Plugin manifest carries version 4.0.0 and v4.0 description tag

`.claude-plugin/plugin.json` (and `marketplace.json` if present) sets
`"version": "4.0.0"` and the `description` field's leading version
reference mentions "v4.0 acceptance-criteria QA cutover" while
preserving the rest of the description body.

### Validation

- **tests-runtime** — pass = `bash tests/unit/plugin-manifest.test.sh` exits 0; fail = the assertion fails (version != 4.0.0 OR description does not contain `v4.0` OR marketplace.json out of sync).
- **tests-smoke** — pass = `bash tests/smoke/working-memory-migration.test.sh` continues to exit 0 (no upstream regression from the bump alone); fail = a smoke regression surfaces.

### Criterion FR-3 — Acceptance-criteria template renders new hierarchy

`templates/acceptance-criteria.md` renders, in order: header → binding
statement → `## User Stories` → per-story (`### US-### → #### Definition
of Done → #### Acceptance Criteria`) → `## Functional Requirements` →
`## Sensor pool` → `## Open Questions`. No AC entry carries an inline
sensor ID; no section classifies sensors at authoring time.

### Validation

- **tests-runtime** — pass = `bash tests/unit/templates-shape.test.sh` exits 0 with all heading-order, US-block, and sensor-pool assertions green; fail = any heading is out of order, any AC entry inlines a sensor ID, or the H1 line does not match `^# Acceptance Criteria — `.
- **lint** — pass = the template's markdown body lints cleanly under whatever markdown linter the host project uses (or, absent one, parses without error in `markdownlint` if available); fail = any new lint warning attributable to this PRD.

### Criterion FR-4 — PRD template strips User Stories block; Discover skill drops US enumeration

`templates/prd.md` no longer carries a `## User Stories` section. The
PRD template's `## Functional Requirements` section is preserved.
`skills/discover/SKILL.md` no longer instructs the dialogue to
enumerate `US-###` and instead points users at
`/yoke:acceptance-criteria` as the US owner.

### Validation

- **tests-runtime** — pass = `bash tests/unit/prd-template-shape.test.sh` and `bash tests/unit/skill-discover-shape.test.sh` both exit 0; fail = either test fails an assertion (orphan US-### heading remains, FR section dropped accidentally, US-### enumeration text remains in discover skill, etc.).

### Criterion FR-5 — Sensor pool resolution helper rejects unresolvable IDs

`lib/sensors/resolve-pool.sh::resolve_sensor_pool <artifact-path>`
exits non-zero when any listed pool ID fails to resolve to
`.yoke/sensors/<id>.md`, with `wm: sensor pool unresolved: <id>` on
stderr per failed ID.

### Validation

- **tests-runtime** — pass = `bash tests/unit/sensor-pool-resolve.test.sh` exits 0 across all-valid / one-unknown / empty fixtures; fail = any branch's assertion fails.

### Criterion FR-6 — Skill body documents pre-flight + resume + grill + materialization

`skills/acceptance-criteria/SKILL.md` documents all five process steps
in declared order: pre-flight (`yoke_require_provider`, PRD/Spec
approved), ≤ 25-line PRD+Tech-Spec resume, lettered-options grill
across five buckets, Quick Round / Full Flow split, materialization
with binding metadata, sensor-pool validation, Trigger 3 menu render
with `next_skill = /yoke:implement`.

### Validation

- **tests-runtime** — pass = `bash tests/unit/skill-acceptance-criteria-shape.test.sh` exits 0; fail = any documented section is missing or out of order.
- **code-review** — pass = `/review` flagged no production-code-style violations in the new SKILL.md (skill bodies are doc-only); fail = the review surfaces logic-bug-equivalent ambiguity in the dialogue contract.

### Criterion FR-7 — Skill end-to-end smoke covers happy / unknown-sensor / Quick-Round paths

`tests/smoke/acceptance-criteria-skill.test.sh` runs the skill
end-to-end against three fixture variants and asserts the documented
behavior for each.

### Validation

- **tests-smoke** — pass = the script exits 0 across all three fixture branches when the script-level driver returns the expected behavior (happy-path produces ratified artifact; unknown-sensor exits non-zero with stderr; Quick-Round converges in fewer turns); fail = any branch behaves out-of-spec.
- **code-review** — pass = the new fixture artifacts and canned-answer scripts pass `/review` for fixture-shape conventions; fail = the review surfaces a fixture-pollution risk (e.g., shared mutable state across fixture branches).

### Criterion FR-8 — Council personas read the new shape and select pool sensors per AC

`agents/sr-qa.md` and `agents/sr-staff.md` reference only the new
artifact shape (US/DoD/AC, sensor pool); each persona prompt carries
a `## Sensor selection at runtime` section instructing the persona
to record per-AC sensor selection in its per-cycle slice file.

### Validation

- **tests-runtime** — pass = `bash tests/runtime/sr-qa-prompt-shape.test.sh` and `bash tests/runtime/sr-staff-prompt-shape.test.sh` both exit 0; fail = legacy `acceptance-contract` references remain or sensor-selection guidance is absent.
- **code-review** — pass = `/review` flagged no anti-scope contradiction (Sr QA still never writes production code; Sr Staff still never invokes `/ultrareview`); fail = the review surfaces an anti-scope drift.

### Criterion FR-9 — Runtime resolves new path; criteria parser emits AC tuples

`lib/runtime/cycle.sh` and `lib/runtime/council.sh` resolve only the
new `.yoke/acceptance-criteria/<slug>.md` path. A new helper
`lib/runtime/criteria-parse.sh::criteria_parse <path>` emits
`<US-ID>|<AC-ID>` tuples one per line on stdout with deterministic
ordering.

### Validation

- **tests-runtime** — pass = `bash tests/runtime/cycle-resolves-criteria-path.test.sh` exits 0 with grep-based assertions on path references and tuple-emission tests against fixture artifacts; fail = any legacy literal remains or a fixture's tuple sequence does not match expected ordering.

### Criterion FR-10 — Arbiter classifies AC-level disagreements as direct contradictions

`agents/council-arbiter.md` enumerates the new artifact hierarchy and
classifies DoD disagreements and AC-level disagreements as direct
contradictions; sensor-selection disagreements remain importance
disagreements; the JSON verdict schema is unchanged.

### Validation

- **tests-sensors** — pass = `bash tests/sensors/council-arbiter.test.sh` exits 0 across all five branch fixtures (existing four plus the new AC-disagreement); fail = the new fixture produces an unexpected verdict shape (e.g., `consensus: true` or sensor-selection-as-direct-contradiction).
- **tests-runtime** — pass = the arbiter prompt's classification table parses against `tests/runtime/arbiter-prompt-shape.test.sh`; fail = the classification rules are absent from the prompt.

### Criterion FR-11 — All documentation surfaces reflect v4.0.0 cutover

`CLAUDE.md` (project), `templates/project-claude-md.md` (host),
`docs/architecture.md`, and `docs/migration-v3-to-v4.md` reference
only `acceptance-criteria` outside intentional historical text. The
project `CLAUDE.md` carries a v4.0.0 entry in "Migration history"
dated 2026-05-03. The migration doc carries the seven required H2
sections.

### Validation

- **tests-runtime** — pass = the four shape tests (`claude-md-shape`, `host-claude-md-template-shape`, `architecture-md-shape`, `migration-doc-shape`) all exit 0; fail = any doc surface retains a stale reference or lacks a required section.

### Criterion FR-12 — Orphan-ref smoke returns zero unaccounted matches

`tests/smoke/orphan-acceptance-contract-refs.test.sh` greps active
code paths (`skills/`, `agents/`, `lib/`, `hooks/`, `templates/`,
`docs/`, `CLAUDE.md`, `.claude-plugin/`) for `acceptance-contract(s)?`
and exits 0 only when the only matches are inside the explicit
allowlist (the migration doc, the "Migration history" block, the
`> **Lineage.**` quote blocks).

### Validation

- **tests-smoke** — pass = the script exits 0 against the live repo after Sprints 1-5 merge AND its self-test fixture at `tests/smoke/fixtures/orphan-ref-positive/` causes the script to exit non-zero when invoked with `--fixture <path>`; fail = any unaccounted match remains in active code paths.

## Applicable policies

This refactor is internal to Yoke's framework shape and does not touch
user data, telemetry, or compliance surfaces. No regulatory policies
(PCI-DSS, LGPD, HIPAA, etc.) apply.

The applicable framework policies — sourced via
`/yoke:search-canonical-memory` and ratified at the canonization step,
not at this contract's binding gate — are:

- **`concepts/yoke-conventions`** — the project-level MUST/MUST-NOT
  policy in canonical memory. The conventions apply to every line
  authored under this PRD; their enforcement is via the existing
  `lint` and `code-review` sensors.
- **English-only language policy** — `CLAUDE.md > Language policy`.
  All in-repo content authored under this PRD is en-US.
- **Path-helper invariant** — `lib/working-memory/paths.sh` is the
  single source of truth for working-memory path resolution; no flat
  literal `.yoke/<dir>/<slug>.md` may be written by skills, agents,
  or libs after this PRD.

---

> Sensor metadata (command, type, token_cost, time_cost, dispatch) lives
> in `.yoke/sensors/<sensor-id>.md` per `templates/sensor.md`.
> `hooks/verify-acceptance.sh` resolves the binding by reading each
> `### Validation` block here plus the per-sensor file pointed to by the
> bullet's `<sensor-id>`.
