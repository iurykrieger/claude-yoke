# Acceptance Criteria — Drop retired-script references from `agents/orchestrator.md`

> Generated by `/yoke:acceptance-criteria` from approved fix-spec + Tech
> Spec on 2026-05-05.
> PRD: `.yoke/fixes/2026-05-05-orchestrator-retired-script-refs.md`
> Tech Spec: `.yoke/specs/2026-05-05-orchestrator-retired-script-refs.md`
> Status: ratified
> Ratified by: iurykrieger
> Ratified at: 2026-05-05T00:00:00Z

> **Binding statement (Trigger 3).** Approving this Acceptance Criteria
> document operationally defines "done" for this task as "passes every
> criterion below". Changes during runtime require a fresh ratification
> round. Sprint contracts negotiated by Sr Eng / Sr QA / Sr Staff at
> runtime can refine interpretation **inside** this envelope but cannot
> relax it. A sprint contract attempting to modify the Acceptance
> Criteria escalates via Trigger 4.

> **Definition of Done vs Acceptance Criteria — distinct, layered.**
> **DoD** is a single binary checklist per User Story that defines when
> the implementation is *finished*. **Acceptance Criteria** are
> multiple observable QA conditions per User Story that, given the
> implementation is done, define when the work is *acceptable* in
> quality terms. The runtime council enforces ordering: DoD checks
> must pass before any AC is evaluated for that story.

> **Sensor pool is unclassified at authoring time.** The
> `## Sensor pool` section below lists every sensor relevant to this
> task as a flat bullet list. Sr QA and Sr Staff pick which pool
> members gate which Acceptance Criterion at Phase 4 runtime, recording
> their selection in their per-cycle slice file. Authoring-time
> mandatory / complementary / optional tags are forbidden — they
> collapse a runtime-policy decision into an authoring-time decision.
> Per-sensor command, type (`computational` | `inferential`),
> `token_cost`, `time_cost`, and dispatch metadata live in
> `.yoke/sensors/<sensor-id>.md` (per `templates/sensor.md`).

## User Stories

### US-001 — Zero retired-script references in the persona prompt

As a Yoke maintainer, I want `agents/orchestrator.md` to contain zero
references to the v2.0.0-retired `canonization-criteria.sh`, so that
an agent loading the persona prompt does not attempt to invoke a
non-existent helper.

#### Definition of Done

- [ ] The four offending paragraphs (formerly anchored at lines 78, 100, 142, and 190 of `agents/orchestrator.md`) are rewritten in place.
- [ ] `grep -cE "canonization-criteria" agents/orchestrator.md` returns `0`.
- [ ] The frontmatter YAML block at lines 1–5 of `agents/orchestrator.md` (`name`, `description`, `tools`) is byte-equivalent to the pre-fix version.

#### Acceptance Criteria

- **AC-001-1:** `grep -cE "canonization-criteria" agents/orchestrator.md` returns `0` and exits `0` after the fix.
- **AC-001-2:** `git diff --name-only HEAD~..HEAD` for the fix's commit returns exactly the single line `agents/orchestrator.md` — no other repository file is modified by the fix.
- **AC-001-3:** `wc -c agents/orchestrator.md` shows a delta of at most 5120 bytes (≤ 0.005 MB) between the pre-fix and post-fix versions.

### US-002 — Explicit `/yoke:canonize` substitution at every formerly-script-named site

As a Yoke maintainer, I want each rewritten paragraph to name
`/yoke:canonize` literally as the dispatch surface, so that the
facade-vs-provider-verbs contract (`concepts/yoke-pattern-facade-vs-provider-verbs`)
is unambiguous in the persona prompt and the Orchestrator subagent
has exactly one verb to invoke.

#### Definition of Done

- [ ] Every rewritten paragraph at the four formerly-script-named sites contains the literal token `/yoke:canonize`.
- [ ] No rewritten paragraph cites an argument or flag of the canonize skill (no `--classify-impact`, no provider-internal script name, no internal phase step beyond the existing "Phase 3" reference).
- [ ] Phase-number references throughout `agents/orchestrator.md` are mutually consistent (no two sites name a different phase number for the same dispatch step).

#### Acceptance Criteria

- **AC-002-1:** Every paragraph that previously contained the substring `canonization-criteria` (formerly at lines 78, 100, 142, 190) now contains the substring `/yoke:canonize` and is internally coherent on its own.
- **AC-002-2:** `grep -nE "canonization-criteria\.sh|--classify-impact" agents/orchestrator.md` returns no matches; no rewritten paragraph names a provider-internal helper or a flag of the canonize skill.
- **AC-002-3:** The post-fix `agents/orchestrator.md` is internally consistent — an LLM-as-judge review confirms the file treats `canonization-criteria.sh` as retired throughout, with `/yoke:canonize` named as the dispatch surface, and surfaces no contradicting clause.

### US-003 — Retirement statements and load-bearing prose preserved

As a Yoke maintainer, I want the v3.0 cutover note and the existing
retirement sentences to remain intact in spirit, plus the
impact-classification keyword-heuristics table preserved verbatim, so
that the historical doctrine context survives the rewrite without
re-introducing the contradiction the fix is meant to remove.

#### Definition of Done

- [ ] The v3.0 cutover note block (the `> **v3.0 cutover note...`
      blockquote at the pre-fix lines 31–54) is byte-equivalent to its
      pre-fix version in the post-fix file.
- [ ] The impact-classification keyword-heuristics table (the
      `| Impact | Trigger keywords | PR behavior |` table at the
      pre-fix lines 104–109) is byte-equivalent to its pre-fix version
      in the post-fix file.
- [ ] At least one sentence in the post-fix file asserts the
      retirement of `canonization-criteria.sh` (semantic match — the
      declarative content "Part 4 of the bedrock canonical-memory port
      retired that primitive" or an equivalent paraphrase survives).

#### Acceptance Criteria

- **AC-003-1:** The v3.0 cutover note blockquote in the post-fix file is byte-equivalent to its pre-fix version (verifiable by extracting the block from both versions and running `diff`).
- **AC-003-2:** The impact-classification keyword-heuristics table in the post-fix file is byte-equivalent to its pre-fix version.
- **AC-003-3:** An LLM-as-judge review of the post-fix file confirms that at least one sentence semantically asserts the retirement of `canonization-criteria.sh`, that the assertion is not contradicted by any other prose in the file, and that the rewrite as a whole reads as a single internally-consistent doctrine document.

## Functional Requirements

- **FR-1:** All modified Markdown files pass the project's existing CommonMark rendering without errors when previewed; specifically, `agents/orchestrator.md` renders without unclosed code fences or malformed list structures (visual validation, decidable by markdown rendering smoke).
- **FR-2:** All Bash scripts under `lib/`, `hooks/`, and `scripts/` continue to pass `shellcheck` with no new warnings introduced by this change set (the doc fix touches no shell, but the project linter must still exit `0` on a fresh CI run that includes the change).
- **FR-3:** The existing CI workflow (`.github/workflows/tests.yml`) exits `0` on a run that includes the fix's commit, with the regression assertion `grep -cE "canonization-criteria" agents/orchestrator.md == 0` either folded into an existing test or runnable inline as part of the standard test pass.

## Sensor pool

- lint
- code-review
- llm-as-judge
- tests-smoke

## Open Questions

None.

---

> When ready, run `/yoke:implement` to advance to Phase 4 (the agent
> council ralph loop on the binding Acceptance Criteria).
