# Acceptance Criteria — Orchestrator → /yoke:canonize handoff

> Generated by `/yoke:acceptance-criteria` from approved fix-spec +
> Tech Spec on 2026-05-05.
> Fix-spec: `.yoke/fixes/2026-05-05-canonize-orchestrator-handoff.md`
> Tech Spec: `.yoke/specs/2026-05-05-canonize-orchestrator-handoff.md`
> Status: ratified
> Ratified by: iury.krieger@stone.com.br
> Ratified at: 2026-05-05T19:16:02Z

> **Binding statement (Trigger 3).** Approving this Acceptance Criteria
> document operationally defines "done" for this task as "passes every
> criterion below". Changes during runtime require a fresh ratification
> round. Sprint contracts negotiated by Sr Eng / Sr QA / Sr Staff at
> runtime can refine interpretation **inside** this envelope but cannot
> relax it. A sprint contract attempting to modify the Acceptance
> Criteria escalates via Trigger 4.

> **Definition of Done vs Acceptance Criteria — distinct, layered.**
> **DoD** is a single binary checklist per User Story that defines when
> the implementation is *finished*. **Acceptance Criteria** are
> multiple observable QA conditions per User Story that, given the
> implementation is done, define when the work is *acceptable* in
> quality terms. The runtime council enforces ordering: DoD checks
> must pass before any AC is evaluated for that story.

> **Sensor pool is unclassified at authoring time.** The
> `## Sensor pool` section below lists every sensor relevant to this
> task as a flat bullet list. Sr QA and Sr Staff pick which pool
> members gate which Acceptance Criterion at Phase 4 runtime, recording
> their selection in their per-cycle slice file.

## User Stories

### US-001 — Aligned canonize-invocation prose with no discriminator

As a Yoke maintainer, I want `agents/orchestrator.md`,
`skills/implement/SKILL.md`, and `skills/canonize/SKILL.md` to
document the canonize-invocation contract identically as a
zero-argument, caller-agnostic verb, so that the contract is
single-authored and future migrations don't drift the three
surfaces apart.

#### Definition of Done

- [ ] `agents/orchestrator.md` no longer mentions `--from-orchestrator` or instructs the Orchestrator to pass the active task's directory path; the Skill-tool dispatch is documented as zero-argument.
- [ ] `skills/implement/SKILL.md` no longer mentions `--from-orchestrator` or instructs the Orchestrator to pass the directory path; the relevant subsection cites the canonize-invocation contract as zero-argument.
- [ ] `skills/canonize/SKILL.md` is unmodified relative to the pre-task `main` (the v2.0.0 facade contract is the authority).
- [ ] No new positional argument, opt-in flag, environment variable, or wrapper subskill is introduced anywhere in the diff.

#### Acceptance Criteria

- **AC-001-1:** `grep -n "from-orchestrator\|--from-orchestrator" agents/orchestrator.md skills/implement/SKILL.md` returns zero matches on the post-task tree.
- **AC-001-2:** `git diff main -- skills/canonize/SKILL.md` is empty (the contract authority file is untouched).
- **AC-001-3:** `grep -nrE 'YOKE_FROM_ORCHESTRATOR|canonize-from-orchestrator' agents/ skills/ lib/ hooks/` returns zero matches on the post-task tree (no env-var or wrapper-subskill discriminator was introduced).
- **AC-001-4:** Markdown lint exits 0 on every modified `.md` file in `agents/` and `skills/implement/` after the prose deltas land.

### US-002 — Orchestrator handoff completes; manual path unchanged

As a `/yoke:implement` coordinator, I want the documented
Orchestrator → `/yoke:canonize` handoff to complete the
five-criterion cascade on `merge-ready` without tripping the v2.0.0
hard-rejection guard, while a manual `/yoke:canonize` invocation
from a developer terminal continues to work end-to-end exactly as
before, so that automated canonization is restored without
regressing the human path.

#### Definition of Done

- [ ] A smoke test exercises the post-fix Orchestrator-shaped invocation of `/yoke:canonize` and asserts the v2.0.0 abort diagnostic is **not** produced.
- [ ] A smoke test (or sub-case in the same test file) exercises a manual zero-argument `/yoke:canonize` invocation and asserts it reaches the provider-dispatch step (or the existing slug-resolution diagnostic on missing `.current`, whichever applies).
- [ ] The active slug consumed by `/yoke:canonize` is read from `.yoke/runtime/.current` via `wm_active_slug`; no alternate slug-resolution path is introduced.

#### Acceptance Criteria

- **AC-002-1:** Running the new smoke test on the post-task tree exits 0 and stdout/stderr contain no occurrence of the literal string `wm: /yoke:canonize takes no arguments at v2.0.0`.
- **AC-002-2:** Running the same smoke test on the pre-task tree (the unfixed code) reproduces the diagnostic literal `wm: /yoke:canonize takes no arguments at v2.0.0` — i.e., the test is load-bearing (Tech Spec FR-7).
- **AC-002-3:** A manual `/yoke:canonize` invocation under a populated `.yoke/runtime/.current` proceeds past the argument-rejection guard (no `wm: /yoke:canonize takes no arguments` diagnostic emitted on the manual path).
- **AC-002-4:** `grep -nE 'wm_active_slug' skills/canonize/SKILL.md` confirms the existing slug-resolution path is unchanged (number of matches is identical pre/post-task).

### US-003 — Regression sensor at `.yoke/sensors/tests-orchestrator-canonize-handoff.md`

As a `/yoke:implement` spawn-time readiness check, I want a new
computational sensor at
`.yoke/sensors/tests-orchestrator-canonize-handoff.md` whose
`command:` invokes the regression smoke test, so that any future
PR re-introducing the retired flag fails closed before Phase 4 can
spawn.

#### Definition of Done

- [ ] `.yoke/sensors/tests-orchestrator-canonize-handoff.md` exists with frontmatter `id: tests-orchestrator-canonize-handoff`, `type: computational`, `token_cost: 0`, `time_cost: 30`, and `command: bash tests/smoke/orchestrator-canonize-handoff.test.sh`.
- [ ] The body contains non-empty `## How to run`, `## Known issues`, and `## Frequent errors` sections (per the sensor template's lint-enforced schema).
- [ ] `.yoke/sensors/tests-orchestrator-canonize-handoff.md` does NOT contain a `## Calibration` section (computational sensors omit it).
- [ ] `tests/smoke/orchestrator-canonize-handoff.test.sh` exists, includes the standard internal watchdog `sleep 600 && kill -TERM $$ &`, and is executable from the repo root via `bash tests/smoke/orchestrator-canonize-handoff.test.sh`.

#### Acceptance Criteria

- **AC-003-1:** `bash lib/sensors/ack-sensors.sh --mode readiness` exits 0 on the post-task tree and lists `tests-orchestrator-canonize-handoff` among the resolved sensors.
- **AC-003-2:** Running the literal `command:` string from the sensor frontmatter (`bash tests/smoke/orchestrator-canonize-handoff.test.sh`) from the repository root with no manual setup exits 0 on the post-task tree.
- **AC-003-3:** Running the same `command:` string against a synthetic regression (a pre-task or rolled-back orchestrator.md/implement.md state that re-introduces the `--from-orchestrator` reference) exits non-zero — the sensor is load-bearing on the regression.
- **AC-003-4:** The sensor file body bullets under `## Frequent errors` reference at least one project-specific failure pattern (the `placeholder:` boilerplate from `templates/sensor.md` is replaced).

## Functional Requirements

- **FR-1:** Canonical memory carries one new doctrine concept entry (authored at `/yoke:canonize`-time after the runtime loop converges) recording the caller-agnostic canonize-invocation decision; the new entry **bidirectionally supersedes** any existing reference to the v1.x `/yoke:preserve --from-orchestrator` shape in `concepts/yoke-pattern-model-c-governance` (Tech Spec FR-6 / Alternatives Considered).
- **FR-2:** Every modified `.md` file in `agents/` and `skills/implement/` passes the project's markdown lint with no new warnings introduced by the diff.
- **FR-3:** Every modified or new bash script under `tests/smoke/` passes `shellcheck` with no new warnings introduced by the diff.
- **FR-4:** The CI pipeline `.github/workflows/tests.yml` exits 0 on the post-task branch (no smoke test in the existing `tests/smoke/` set is broken by the new test or the prose deltas).
- **FR-5:** The change set introduces zero new files outside the documented surface — `agents/orchestrator.md`, `skills/implement/SKILL.md`, `.yoke/sensors/tests-orchestrator-canonize-handoff.md`, and `tests/smoke/orchestrator-canonize-handoff.test.sh` are the only paths touched in the implementation diff (working-memory archive files under `.yoke/` are exempt).

## Sensor pool

- tests-smoke
- tests-sensors
- tests-canonical-memory
- lint
- code-review

## Open Questions

None.

---

> When ready, run `/yoke:implement` to advance to Phase 4 (the agent
> council ralph loop on the binding Acceptance Criteria).
