---
task_id: 2026-05-03-acceptance-criteria-refactor-s01
sprint: 1
slug: 2026-05-03-acceptance-criteria-refactor
status: approved
created_at: 2026-05-03T06:44:00Z
model: ""
traceability: ""
Migrated-from: []
---

# Sprint 01 of 05: Path scaffolding and skill/directory rename

> Generated by `/yoke:tech-spec` (stage 3) from
> `.yoke/specs/2026-05-03-acceptance-criteria-refactor.md`. Status flips to `approved` when Trigger 2
> approves the parent spec.

## Sprint objective

Land the cutover scaffolding for Yoke v4.0.0 in a single coherent
shippable increment: the new path helper `wm_acceptance_criteria_path`
exists in `lib/working-memory/paths.sh` and the legacy
`wm_acceptance_contract_path` is deleted (no delegation, no shim);
`templates/acceptance-criteria.md` exists carrying the legacy template
body verbatim (reshape ships Sprint 2); `skills/acceptance-criteria/SKILL.md`
exists at the new path with the legacy skill body retained except for
the title and the verb references that point at the new directory
(behavior rewrite ships Sprint 3); the plugin manifest version bumps
to `4.0.0` in `.claude-plugin/plugin.json` (and `marketplace.json` if
it carries a parallel field). After this sprint the cutover bones are
in place and Sprints 2-5 can edit content in stable paths without
further rename churn.

## Sprint DoD

- `grep -qE '^wm_acceptance_criteria_path\(\)' lib/working-memory/paths.sh` exits 0.
- `! grep -qE '^wm_acceptance_contract_path\(\)' lib/working-memory/paths.sh` (legacy helper deleted).
- `test -f templates/acceptance-criteria.md && test ! -f templates/acceptance-contract.md`.
- `test -d skills/acceptance-criteria && test ! -d skills/acceptance-contract`.
- `grep -q '"version": "4.0.0"' .claude-plugin/plugin.json`.
- `bash tests/unit/working-memory-paths.test.sh` exits 0.
- `bash tests/unit/template-files.test.sh` exits 0.
- `bash tests/unit/skill-files.test.sh` exits 0.
- `bash tests/unit/plugin-manifest.test.sh` exits 0.

## Tasks

### Task 2026-05-03-acceptance-criteria-refactor-s01-t01

**Story:** Add `wm_acceptance_criteria_path <slug>` and delete `wm_acceptance_contract_path` so every code path that touches the artifact location goes through one well-defined helper and stale callers fail loudly.

**Technical implementation:** Edit `lib/working-memory/paths.sh`. Add `wm_acceptance_criteria_path` mirroring the existing `wm_acceptance_contract_path` shape (slug validation via `wm_validate_slug`, returns `.yoke/acceptance-criteria/<slug>.md` on stdout, non-zero on invalid slug). Delete `wm_acceptance_contract_path` outright (no delegation, no warning). Audit and remove sibling helpers tied to the legacy name (e.g., `wm_list_acceptance_contract_paths` if present). Cite `concepts/yoke-pattern-plugin-structure` and `concepts/yoke-conventions` (path helpers as central indirection — the file is the single source of truth for working-memory paths).

**Validation:** Add `tests/unit/working-memory-paths.test.sh` (or extend if it exists) covering: (a) `wm_acceptance_criteria_path "2026-05-03-foo"` outputs `.yoke/acceptance-criteria/2026-05-03-foo.md` and exits 0; (b) `wm_acceptance_criteria_path "invalid slug"` exits non-zero; (c) `bash -c 'source lib/working-memory/paths.sh; type wm_acceptance_contract_path 2>&1'` returns non-zero with `not found` (deletion confirmed). Test must run under `/opt/homebrew/bin/bash` (bash 4+) per macOS dev-env note in `CLAUDE.md`.

**Acceptance criterion:** `bash tests/unit/working-memory-paths.test.sh` exits 0 AND `grep -qE '^wm_acceptance_contract_path\(\)' lib/working-memory/paths.sh` exits non-zero.

### Task 2026-05-03-acceptance-criteria-refactor-s01-t02

**Story:** Rename `templates/acceptance-contract.md` to `templates/acceptance-criteria.md` (verbatim body, only the title-line `# Acceptance Contract — <task name>` becomes `# Acceptance Criteria — <task name>`) so Sprint 2 can edit one file in place rather than juggling two.

**Technical implementation:** `git mv templates/acceptance-contract.md templates/acceptance-criteria.md`. Edit only the H1 title to `# Acceptance Criteria — <task name>`. Do not touch the body — full reshape ships Sprint 2. Verify `git log --diff-filter=R --follow templates/acceptance-criteria.md` shows the rename as a rename, not a delete-and-add (preserves blame).

**Validation:** Add `tests/unit/template-files.test.sh` asserting (a) `test -f templates/acceptance-criteria.md`; (b) `test ! -f templates/acceptance-contract.md`; (c) the file's first line matches `^# Acceptance Criteria — `; (d) `git log --diff-filter=R --follow templates/acceptance-criteria.md | head -1` returns a non-empty `commit` line (the rename is in history).

**Acceptance criterion:** `bash tests/unit/template-files.test.sh` exits 0.

### Task 2026-05-03-acceptance-criteria-refactor-s01-t03

**Story:** Rename `skills/acceptance-contract/` to `skills/acceptance-criteria/` (carrying the legacy skill body verbatim except for the H1 title and any internal verb references) so command-palette discovery routes to the new skill from this sprint forward; behavior rewrite ships Sprint 3.

**Technical implementation:** `git mv skills/acceptance-contract skills/acceptance-criteria`. Edit `skills/acceptance-criteria/SKILL.md`: replace H1 title `# /yoke:acceptance-contract` with `# /yoke:acceptance-criteria`; replace any inline mentions of the verb `/yoke:acceptance-contract` that reference the *current* skill with `/yoke:acceptance-criteria` (preserve historical mentions inside `Lineage.` quote blocks). Do NOT rewrite the dialogue/process flow — that ships Sprint 3. Update `.claude-plugin/plugin.json` `skills:` block if it lists the skill path explicitly (verify with `jq '.skills'`); update `.claude-plugin/marketplace.json` analogously if applicable.

**Validation:** Add `tests/unit/skill-files.test.sh` asserting (a) `test -f skills/acceptance-criteria/SKILL.md`; (b) `test ! -d skills/acceptance-contract`; (c) the skill's H1 line matches `^# /yoke:acceptance-criteria`; (d) `find skills -type d -name 'acceptance-contract*'` returns empty.

**Acceptance criterion:** `bash tests/unit/skill-files.test.sh` exits 0.

### Task 2026-05-03-acceptance-criteria-refactor-s01-t04

**Story:** Bump plugin version to `4.0.0` in `.claude-plugin/plugin.json` (and `marketplace.json` if applicable) so host projects pinned on `claude-yoke@3.x` see the breaking change as a major version bump per semver.

**Technical implementation:** Edit `.claude-plugin/plugin.json`: set `"version": "4.0.0"`; update the `description` field's leading version reference to mention "v4.0 acceptance-criteria QA cutover" while preserving the rest (council protocol, canonical-memory pluggability narrative). If `.claude-plugin/marketplace.json` carries a parallel `version` field, update it identically.

**Validation:** Add `tests/unit/plugin-manifest.test.sh` asserting (a) `jq -r .version .claude-plugin/plugin.json` returns `4.0.0`; (b) `jq -r .description .claude-plugin/plugin.json` contains the literal substring `v4.0`; (c) if marketplace.json exists, its version equals plugin.json's version. Smoke regression: `bash tests/smoke/working-memory-migration.test.sh` continues to exit 0 (no upstream regression from the bump alone).

**Acceptance criterion:** `bash tests/unit/plugin-manifest.test.sh` exits 0.

## Functional acceptance criteria

- `wm-acceptance-criteria-path-callable`
- `legacy-acceptance-contract-helper-deleted`
- `template-renamed-to-acceptance-criteria`
- `skill-renamed-to-acceptance-criteria`
- `plugin-version-4-0-0`

## Sensors

- `lint`
- `tests-runtime`
- `tests-smoke`

---

> Generated by `/yoke:tech-spec` (stage 3) from `.yoke/specs/2026-05-03-acceptance-criteria-refactor.md`.
> Status flips to `approved` when Trigger 2 approves the parent spec.
