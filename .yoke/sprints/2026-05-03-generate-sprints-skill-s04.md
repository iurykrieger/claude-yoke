---
task_id: 2026-05-03-generate-sprints-skill-s04
sprint: 4
slug: 2026-05-03-generate-sprints-skill
status: approved
created_at: 2026-05-03T06:45:19Z
model: claude-opus-4-7-1m
traceability: .yoke/specs/2026-05-03-generate-sprints-skill.md#sprint-4
Migrated-from: []
---

# Sprint 04 of 04: Cutover, integration, legacy, tests, docs

> Generated by `/yoke:tech-spec` (stage 3) from
> `.yoke/specs/2026-05-03-generate-sprints-skill.md`. Status flips to
> `approved` when Trigger 2 approves the parent spec.

## Sprint objective

Cut over the producer split: strip sprint generation from
`/yoke:tech-spec` (delete stage 2 + stage 3 + the Tech-Spec-only
per-sprint approval-menu block, retarget chain to
`/yoke:acceptance-criteria`); add the new gate state
`awaiting:generate-sprints` to `/yoke:status`; teach `/yoke:implement`
to refuse on that state; codify legacy coexistence (presence of
`.yoke/acceptance-criteria/<slug>.md` selects the new flow; absence
selects the legacy flow); ship the full-flow smoke test that walks
`/yoke:discover` → `/yoke:tech-spec` → `/yoke:acceptance-criteria` →
`/yoke:generate-sprints` → first `/yoke:implement` cycle dry-run; and
update `docs/architecture.md` (Phase 2.5 section), `docs/lineage.md`
(mark `skills/generate-sprints` as native), and stage the
canonical-memory handoff packet (`concepts/yoke-pattern-sprint-synthesis`
plus the Trigger 2.5 amendment to `concepts/yoke-pattern-human-triggers`).
After this sprint, the new flow is the default for new tasks; legacy
tasks keep walking unchanged.

## Sprint DoD

- `grep -RIn 'sprints/' skills/tech-spec/` returns zero matches (sprint generation removed).
- `grep -RIn 'scaffold-sprints' skills/tech-spec/` returns zero matches.
- `grep -RIn 'awaiting:generate-sprints' skills/status/` returns ≥ 1 match (new state surfaced by `/yoke:status`).
- `grep -RIn 'awaiting:generate-sprints' skills/implement/` returns ≥ 1 match (consumer refuses on new state).
- `tests/runtime/full-flow.test.sh` exits 0.
- `tests/smoke/legacy-task-walk.test.sh` exits 0.
- `tests/smoke/generate-sprints-rejects-legacy.test.sh` exits 0.
- `grep -q 'Phase 2\.5' docs/architecture.md` returns 0.
- `grep -q 'skills/generate-sprints' docs/lineage.md` returns 0.
- `grep -q 'yoke-pattern-sprint-synthesis' .yoke/runtime/.preserve-packet.md` returns 0.
- `grep -q 'Trigger 2\.5' .yoke/runtime/.preserve-packet.md` returns 0.

## Tasks

### Task 2026-05-03-generate-sprints-skill-s04-t01

**Story:** As the post-cutover `/yoke:tech-spec`, I produce only the architecture spec at `.yoke/specs/<slug>.md` and never write sprint files, so the producer-side decoupling is realized end-to-end. (Realizes: US-001)

**Technical implementation:** Edit `skills/tech-spec/SKILL.md`. Delete the entire 3-stage blueprint preamble and replace it with a single-stage description: Stage 1 (LLM) drafts `.yoke/specs/<slug>.md` end-to-end (architecture, contracts, dependencies, out-of-scope) — that is the entire skill output. Delete every reference to `lib/working-memory/scaffold-sprints.sh`. Delete every Stage 3 per-sprint fill loop. Delete the Tech-Spec-only per-sprint summary block in the approval-menu invocation. Update `next_skill` from `/yoke:acceptance-contract` to `/yoke:acceptance-criteria` (the renamed Phase 3 skill). Update the `templates/spec.md` reference to drop the per-sprint heading shape — the new spec template is sprint-less (architecture only). Update `templates/spec.md` accordingly: remove the `### Sprint <NN> — <name>` example and the `**Tasks:** see ...` line; replace with a single `## Sprints (deferred)` paragraph noting that sprint partitioning happens in Phase 2.5 via `/yoke:generate-sprints`. Mirror the change in `templates/approval-menu.md` Tech-Spec-only block: gate the block on the *legacy* shape and document its retirement in the new flow.

**Validation:** Run the legacy-task fixture through `/yoke:tech-spec` and assert `find .yoke/sprints -name '<fixture-slug>-s*.md' -newer <pre>` is empty. Run the new-flow fixture through `/yoke:tech-spec` and assert `.yoke/specs/<fixture-slug>.md` exists, is approved, and contains zero `### Task <slug>-s` headings.

**Acceptance criterion:** `grep -RIn 'sprints/' skills/tech-spec/` returns zero matches AND `grep -RIn 'scaffold-sprints' skills/tech-spec/` returns zero matches AND running `/yoke:tech-spec` against the new-flow fixture produces no file under `.yoke/sprints/`.

### Task 2026-05-03-generate-sprints-skill-s04-t02

**Story:** As `/yoke:status`, I report the new gate state `awaiting:generate-sprints` when a task has approved spec + approved acceptance-criteria but zero sprint files, so the user can see what to do next without guessing. (Realizes: US-006)

**Technical implementation:** Edit `skills/status/SKILL.md`. Add the new state to the gate-detection ladder, in priority order: `awaiting:tech-spec` (no spec or unapproved spec) → `awaiting:acceptance-criteria` (spec approved, AC missing or unapproved) → **NEW:** `awaiting:generate-sprints` (spec + AC approved, but `find .yoke/sprints -name '<slug>-s*.md' | wc -l == 0`) → `running:implement` (sprint files exist, status approved, `progress.md` shows in-flight cycles) → `done` (all sprints completed). Lift the legacy-flow detection rule (presence of `.yoke/acceptance-contracts/<slug>.md` instead of `.yoke/acceptance-criteria/<slug>.md`) into a single switch — when the legacy flag is set, the ladder collapses to the legacy 4-state machine (`awaiting:tech-spec` → `awaiting:acceptance-contract` → `running:implement` → `done`). The status output renders the active state plus a one-line action hint (e.g., `awaiting:generate-sprints — run /yoke:generate-sprints to advance`).

**Validation:** Smoke test `tests/smoke/status-awaiting-generate-sprints.test.sh` engineers a fixture in the documented state (approved spec + approved AC + zero sprint files) and asserts `/yoke:status` stdout contains the literal `awaiting:generate-sprints`. A second branch engineers a legacy-flow fixture (acceptance-contracts/, no acceptance-criteria/) and asserts the legacy ladder is selected.

**Acceptance criterion:** `bash tests/smoke/status-awaiting-generate-sprints.test.sh` exits 0 AND stdout contains both `PASS: new-flow gate state surfaced` and `PASS: legacy ladder selected on legacy fixture`.

### Task 2026-05-03-generate-sprints-skill-s04-t03

**Story:** As `/yoke:implement`, I refuse to run when the active task is in state `awaiting:generate-sprints`, surfacing a `wm:`-prefixed stderr that names the next skill to run, so the user is never silently advanced into Phase 4 without sprint files. (Realizes: US-006)

**Technical implementation:** Edit `skills/implement/SKILL.md` and the cycle-driver helpers under `lib/runtime/` (specifically the entry-point script in `lib/ralph-loop/orchestrate.sh` or whichever holds the pre-cycle gate check). Add a pre-cycle check that resolves the active gate state (reuse the gate-detection ladder from t02 — lift it into a shared helper `lib/working-memory/gate-state.sh :: detect_gate_state`). On `awaiting:generate-sprints`, abort non-zero with the literal stderr `wm: run /yoke:generate-sprints to advance to Phase 4` and exit cleanly without entering any cycle. The check fires AFTER the existing `current_sprint:` resolution logic, so legacy tasks (no AC file) keep walking through the legacy gate ladder unaffected.

**Validation:** Smoke test `tests/smoke/implement-refuses-on-awaiting.test.sh` engineers a new-flow fixture in the documented state and asserts `/yoke:implement` exits non-zero with stderr containing the exact literal `wm: run /yoke:generate-sprints to advance to Phase 4`. A second branch engineers a legacy-flow fixture with sprint files present and asserts `/yoke:implement` enters its first cycle without abort.

**Acceptance criterion:** `bash tests/smoke/implement-refuses-on-awaiting.test.sh` exits 0 AND stdout contains both `PASS: new-flow refusal correct` and `PASS: legacy walks unaffected`.

### Task 2026-05-03-generate-sprints-skill-s04-t04

**Story:** As a Yoke user re-running `/yoke:generate-sprints` against a legacy task, I want the skill to detect the legacy flow and reject with a clear stderr message so I know automatic migration is not supported. (Realizes: US-007)

**Technical implementation:** Edit `skills/generate-sprints/SKILL.md`. After Pre-flight gate (1) but BEFORE gate (4) (which checks acceptance-criteria), add a legacy-detection branch: if `.yoke/acceptance-contracts/<slug>.md` exists OR `.yoke/sprints/<slug>-s*.md` files already exist with frontmatter `traceability` lacking the new-flow marker (no `acceptance-criteria/<slug>.md` substring), declare the task legacy. On legacy detection, abort non-zero with the literal stderr `wm: legacy task — generate-sprints does not migrate` and exit cleanly. Do NOT touch any file under `.yoke/sprints/`. Cite `concepts/yoke-pattern-memory-model` and the PRD's Decision 6A (no automatic migration) inline as a comment.

**Validation:** Smoke test `tests/smoke/generate-sprints-rejects-legacy.test.sh` engineers a legacy-flow fixture (acceptance-contracts file present, sprint files emitted by legacy `/yoke:tech-spec`) and asserts the skill exits non-zero with the documented stderr literal AND no file under `.yoke/sprints/` was touched (mtime check).

**Acceptance criterion:** `bash tests/smoke/generate-sprints-rejects-legacy.test.sh` exits 0 AND stdout contains `PASS: legacy task rejected, sprints untouched`.

### Task 2026-05-03-generate-sprints-skill-s04-t05

**Story:** As a CI run, I want a single full-flow smoke that walks the whole new-flow chain end-to-end so any wiring regression across phases is caught before merge. (Realizes: US-008)

**Technical implementation:** Create `tests/runtime/full-flow.test.sh`. The test walks: (1) `/yoke:discover` against a synthetic idea string, simulating user replies to drive the dialogue to PRD approval; (2) `/yoke:tech-spec` to draft + approve the architecture spec; (3) `/yoke:acceptance-criteria` to draft + approve UCs (≥ 2); (4) `/yoke:generate-sprints` to synthesize, partition, render, and approve sprint files (≥ 1 sprint produced); (5) `/yoke:implement` first-cycle dry-run that invokes the council Phase A but does NOT spawn the actual personas (set an env var or use a fixture flag the runtime respects to short-circuit after the gate checks pass). Final assertions: every artifact exists at the documented path, every artifact's frontmatter `status` is `approved`, `progress.md` was created, `current_sprint: 1` is set. Watchdog pattern (`sleep 600 && kill -TERM $$ &`). Test runs in CI on every PR.

**Validation:** Run the test directly. The test takes minutes (the simulated dialogues are non-trivial); the watchdog protects against runaway. Inspect intermediate state for one diagnostic round before relying on the test in CI.

**Acceptance criterion:** `bash tests/runtime/full-flow.test.sh` exits 0 AND the test prints a final summary `PASS: full-flow walked discover → tech-spec → acceptance-criteria → generate-sprints → implement-dry-run`.

### Task 2026-05-03-generate-sprints-skill-s04-t06

**Story:** As a CI run, I want a smoke test that exercises a legacy task end-to-end under `/yoke:implement`, so the legacy coexistence guarantee is enforced. (Realizes: US-007, US-008)

**Technical implementation:** Create `tests/smoke/legacy-task-walk.test.sh`. Use `tests/fixtures/legacy-task/` (a frozen snapshot extracted from the `2026-05-01-agent-council` task or an equivalent legacy task: legacy spec + legacy `acceptance-contracts/<slug>.md` + legacy sprint files in `.yoke/sprints/`). The test invokes `/yoke:implement` against this fixture and asserts: (a) the legacy gate ladder is selected (no `awaiting:generate-sprints` state seen); (b) `/yoke:implement` enters its first cycle without abort; (c) the council Phase A pre-spawn step succeeds (we don't spawn the actual personas — short-circuit after the spawn-prep step completes). Watchdog pattern.

**Validation:** Run the test against the frozen fixture. Inspect runtime state under `.yoke/runtime/` to confirm the legacy ladder selection is logged.

**Acceptance criterion:** `bash tests/smoke/legacy-task-walk.test.sh` exits 0 AND stdout contains `PASS: legacy task walked Phase A pre-spawn`.

### Task 2026-05-03-generate-sprints-skill-s04-t07

**Story:** As a Yoke maintainer reading `docs/architecture.md`, I find a Phase 2.5 section that describes the synthesis flow and references both tech-spec and acceptance-criteria artifacts, so the framework's self-description is current. (Realizes: US-008)

**Technical implementation:** Edit `docs/architecture.md`. Insert a new H2 section `## Phase 2.5 — Sprint synthesis` between the existing Phase 2 and Phase 3 sections. Body: describe `/yoke:generate-sprints` as the post-rename Phase 2.5 producer; cite the new flow `/yoke:discover` → `/yoke:tech-spec` → `/yoke:acceptance-criteria` → `/yoke:generate-sprints` → `/yoke:implement`; describe the Trigger 2.5 gate (sprint plan ratification, non-binding for criteria); cite `concepts/yoke-pattern-sprint-synthesis` (the canonical-memory entry to be staged in t08); cite the legacy-coexistence rule (presence of `.yoke/acceptance-criteria/<slug>.md` selects the new flow). Add a one-line cut-over note: tasks created on/after the new-flow ratification date use the new flow; older tasks finish under the legacy flow. Edit `docs/lineage.md`: add `skills/generate-sprints` to the table with `Lineage: native (no upstream)` and a one-paragraph rationale (the producer split required a new skill, not a fork; the canonical-memory facade and the approval-menu template are reused).

**Validation:** Manual inspection. Grep assertions: `grep -q 'Phase 2\.5' docs/architecture.md`, `grep -q '/yoke:generate-sprints' docs/architecture.md`, `grep -q 'skills/generate-sprints' docs/lineage.md`.

**Acceptance criterion:** `grep -c 'Phase 2\.5' docs/architecture.md` returns 0 status with stdout integer ≥ 1 AND `grep -c 'skills/generate-sprints' docs/lineage.md` returns 0 status with stdout integer ≥ 1.

### Task 2026-05-03-generate-sprints-skill-s04-t08

**Story:** As the Orchestrator subagent at full-run termination, I find a staged canonical-memory handoff packet at `.yoke/runtime/.preserve-packet.md` that proposes the new pattern entry `concepts/yoke-pattern-sprint-synthesis` plus the Trigger 2.5 amendment to `concepts/yoke-pattern-human-triggers`, so the canonize cascade has the doctrine update ready for ratification. (Realizes: US-008)

**Technical implementation:** Create `.yoke/runtime/.preserve-packet.md` (gitignored runtime state; pre-populated by Sprint 4 manually, since the new flow is not yet running on itself for this task). Two candidate entries: (1) **`concepts/yoke-pattern-sprint-synthesis`** — full body describing the producer split, the synthesis stage as a bracketed agentic node, the deterministic partition stage, the bundle render stage, the Trigger 2.5 gate, the legacy-coexistence rule. Frontmatter: `type: concept`, `category: pattern`, `status: active`, `ratified: 2026-05-03`, `traceability: .yoke/prds/2026-05-03-generate-sprints-skill.md`, `applies_to: [skills/generate-sprints, skills/tech-spec, skills/acceptance-criteria, skills/implement, skills/status, lib/generate-sprints/]`, `refines: [yoke-pattern-sprint-runtime-bundle]`, `model_calibrated_against: claude-opus-4-7-1m`, `impact_level: high`. (2) **`concepts/yoke-pattern-human-triggers` amendment** — append a Trigger 2.5 entry to the existing trigger list (in canonical memory; the working-memory packet captures the proposed delta as a unified diff or a `before/after` block). Cite the cut-over date as the new-flow ratification anchor. Update the active project entity `projects/claude-yoke` action-items with the new flow's adoption.

**Validation:** Grep `.yoke/runtime/.preserve-packet.md` for the two pattern names; assert both present. The packet is consumed by the Orchestrator at `/yoke:canonize` time, not by this sprint directly; this task ships the packet *content*, not the canonization itself.

**Acceptance criterion:** `grep -c 'yoke-pattern-sprint-synthesis' .yoke/runtime/.preserve-packet.md` returns 0 status with stdout integer ≥ 1 AND `grep -c 'Trigger 2\.5' .yoke/runtime/.preserve-packet.md` returns 0 status with stdout integer ≥ 1.

## Functional acceptance criteria

- tech-spec-no-sprint-output
- spec-template-sprintless
- status-awaiting-generate-sprints-state
- implement-refuses-on-awaiting-state
- generate-sprints-rejects-legacy-task
- full-flow-smoke-green
- legacy-task-walks-clean
- architecture-doc-mentions-phase-25
- lineage-doc-mentions-generate-sprints
- canonize-packet-stages-pattern-entry
- canonize-packet-stages-trigger-25-amendment

## Sensors

- tests-smoke
- tests-runtime
- tests-sensors
- code-review
- lint

---

> Generated by `/yoke:tech-spec` (stage 3) from `.yoke/specs/2026-05-03-generate-sprints-skill.md`.
> Status flips to `approved` when Trigger 2 approves the parent spec.
