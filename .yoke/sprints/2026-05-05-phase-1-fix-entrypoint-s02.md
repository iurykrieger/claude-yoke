<stdin>:87: DeprecationWarning: datetime.datetime.utcnow() is deprecated and scheduled for removal in a future version. Use timezone-aware objects to represent datetimes in UTC: datetime.datetime.now(datetime.UTC).
---
task_id: 2026-05-05-phase-1-fix-entrypoint-s02
sprint: 2
slug: 2026-05-05-phase-1-fix-entrypoint
status: approved
created_at: 2026-05-05T13:56:50Z
model: ""
traceability: ".yoke/specs/2026-05-05-phase-1-fix-entrypoint.md; .yoke/acceptance-criteria/2026-05-05-phase-1-fix-entrypoint.md"
Migrated-from: []
---

# Sprint 02 of <MM>: <one-line sprint name>

> Generated by `/yoke:tech-spec` (stage 3) from
> `.yoke/specs/2026-05-05-phase-1-fix-entrypoint.md`. Status flips to `approved` when Trigger 2
> approves the parent spec.
>
> Sprint files are runtime bundles, not sources of truth. Sources of
> truth (Acceptance Contract criteria, sensor logic) stay authoritative
> in their own files; this sprint file references them by ID and never
> inlines their bodies. Cite
> `concepts/yoke-pattern-memory-model` and the new
> `concepts/yoke-pattern-sprint-runtime-bundle` (drafted in sprint 4's
> preserve packet of the sprint-as-cycle PRD) for the load-bearing
> invariants.
>
> The frontmatter `task_id` field intentionally keeps the legacy name
> for compatibility with downstream tooling that treats every working-
> memory artifact uniformly. Its value is the sprint id (`2026-05-05-phase-1-fix-entrypoint-s02`).
> The optional `Migrated-from:` array carries original `2026-05-05-phase-1-fix-entrypoint-part-N.md`
> or `2026-05-05-phase-1-fix-entrypoint-s02-t<MM>.md` paths when this sprint file is the result
> of the sprint-as-cycle PRD's migration sprints.

## Sprint objective

Realize US-001, US-003, US-004 anchored on 'fr-9'.

## Sprint DoD

- 2026-05-05-phase-1-fix-entrypoint-s02-t01: AC-004-1, AC-004-2: when both Phase-1 archives exist for a slug, the resolver emits the structured diagnostic and exits non-zero; when neither exists, it emits the neither-case diagnostic and exits non-zero.
- 2026-05-05-phase-1-fix-entrypoint-s02-t02: AC-004-3: wm_set_active invoked with a slug whose archives contain both files refuses to record .current, prints the FR-9 diagnostic, and leaves .current unmodified.
- 2026-05-05-phase-1-fix-entrypoint-s02-t03: AC-003-1, AC-003-2: grep -rE 'wm_prd_path' skills/ filtered against discover/fix returns zero matches; existing PRD-backed regression suite passes end-to-end through every migrated skill.
- 2026-05-05-phase-1-fix-entrypoint-s02-t04: AC-003-1: the sensor exits 0 when only discover and fix skills read wm_prd_path; exits non-zero with structured diagnostic when any other skill reads it.
- 2026-05-05-phase-1-fix-entrypoint-s02-t05: AC-004-4: the no-ambiguous-phase1 sensor fails CI on any test run that emits 'wm: ambiguous Phase-1 state'.
- 2026-05-05-phase-1-fix-entrypoint-s02-t06: AC-001-1, AC-003-2, AC-003-3: the happy-path smoke passes for fix-spec invocations; the regression suite passes for ≥ 5 PRD-backed scenarios; the fix-spec-end-to-end smoke completes the full lifecycle without path errors.

## Tasks

### Task 2026-05-05-phase-1-fix-entrypoint-s02-t01

**Story:** Add wm_phase1_artifact_path "<slug>" to lib/working-memory/paths.sh as the canonical existence-aware resolver. The helper checks whether .yoke/prds/<slug>.md and/or .yoke/fixes/<slug>.md exist on disk; returns the existing path on the unambiguous case (exactly one); hard-aborts non-zero with the verbatim wm: ambiguous Phase-1 state diagnostic when both exist (paths + first-100-bytes excerpts + last-commit sha + author + ISO-8601 timestamp + git rm recipe); hard-aborts with the wm: prefix and the literal remediation 'Run /yoke:discover or /yoke:fix first.' when neither exists. The function's docstring MUST state explicitly that the existence-aware contract does not propagate to future helpers, which remain pure path computers by default — this is the canonical I/O-aware exception in paths.sh. Add unit tests under tests/lib/ covering all four branches (neither / only-PRD / only-fix / both), the diagnostic substring assertion 'wm: ambiguous Phase-1 state for slug ', the exit-code assertion (non-zero on neither/both, zero on exactly one), and the read-vs-write symmetry (no flag distinguishing read or write callers). (Realizes: US-003, US-004)

**Technical implementation:** Add wm_phase1_artifact_path "<slug>" to lib/working-memory/paths.sh as the canonical existence-aware resolver. The helper checks whether .yoke/prds/<slug>.md and/or .yoke/fixes/<slug>.md exist on disk; returns the existing path on the unambiguous case (exactly one); hard-aborts non-zero with the verbatim wm: ambiguous Phase-1 state diagnostic when both exist (paths + first-100-bytes excerpts + last-commit sha + author + ISO-8601 timestamp + git rm recipe); hard-aborts with the wm: prefix and the literal remediation 'Run /yoke:discover or /yoke:fix first.' when neither exists. The function's docstring MUST state explicitly that the existence-aware contract does not propagate to future helpers, which remain pure path computers by default — this is the canonical I/O-aware exception in paths.sh. Add unit tests under tests/lib/ covering all four branches (neither / only-PRD / only-fix / both), the diagnostic substring assertion 'wm: ambiguous Phase-1 state for slug ', the exit-code assertion (non-zero on neither/both, zero on exactly one), and the read-vs-write symmetry (no flag distinguishing read or write callers).

**Validation:** (no validation provided)

**Acceptance criterion:** AC-004-1, AC-004-2: when both Phase-1 archives exist for a slug, the resolver emits the structured diagnostic and exits non-zero; when neither exists, it emits the neither-case diagnostic and exits non-zero.

### Task 2026-05-05-phase-1-fix-entrypoint-s02-t02

**Story:** Extend wm_set_active in lib/working-memory/paths.sh with the write-time invariant gate: before writing the slug to .yoke/runtime/.current, the helper checks whether both .yoke/prds/<slug>.md and .yoke/fixes/<slug>.md exist; if so, it refuses to record the slug, prints the FR-9 ambiguous-Phase-1-state diagnostic verbatim to stderr, exits non-zero, and leaves .yoke/runtime/.current unchanged. This is belt-and-suspenders against scenarios I (cross-branch merge collision) and V (manual merge resolution accepting both) which bypass FR-4's slug-collision check. Update the existing unit tests for wm_set_active to add a regression case for the both-exist refusal; preserve every existing happy-path case verbatim. (Realizes: US-004)

**Technical implementation:** Extend wm_set_active in lib/working-memory/paths.sh with the write-time invariant gate: before writing the slug to .yoke/runtime/.current, the helper checks whether both .yoke/prds/<slug>.md and .yoke/fixes/<slug>.md exist; if so, it refuses to record the slug, prints the FR-9 ambiguous-Phase-1-state diagnostic verbatim to stderr, exits non-zero, and leaves .yoke/runtime/.current unchanged. This is belt-and-suspenders against scenarios I (cross-branch merge collision) and V (manual merge resolution accepting both) which bypass FR-4's slug-collision check. Update the existing unit tests for wm_set_active to add a regression case for the both-exist refusal; preserve every existing happy-path case verbatim.

**Validation:** (no validation provided)

**Acceptance criterion:** AC-004-3: wm_set_active invoked with a slug whose archives contain both files refuses to record .current, prints the FR-9 diagnostic, and leaves .current unmodified.

### Task 2026-05-05-phase-1-fix-entrypoint-s02-t03

**Story:** Migrate the five downstream skills (/yoke:tech-spec, /yoke:acceptance-criteria, /yoke:generate-sprints, /yoke:implement, /yoke:canonize, /yoke:status) from wm_prd_path to wm_phase1_artifact_path for Phase-1-artifact reads. Replace every direct wm_prd_path call site that previously read the user-task PRD with the new resolver. Update each skill's pre-flight error message from 'Run /yoke:discover first.' to 'Run /yoke:discover or /yoke:fix first.' (or equivalent — verbatim user-facing strings need explicit edits). After the migration, every wm_prd_path call site outside skills/discover/SKILL.md and skills/fix/SKILL.md MUST be removed; this is enforced by the new no-direct-prd-path sensor. Do not branch any skill's core logic on which artifact backed the slug — the artifact is read as opaque Phase-1 input. (Realizes: US-003)

**Technical implementation:** Migrate the five downstream skills (/yoke:tech-spec, /yoke:acceptance-criteria, /yoke:generate-sprints, /yoke:implement, /yoke:canonize, /yoke:status) from wm_prd_path to wm_phase1_artifact_path for Phase-1-artifact reads. Replace every direct wm_prd_path call site that previously read the user-task PRD with the new resolver. Update each skill's pre-flight error message from 'Run /yoke:discover first.' to 'Run /yoke:discover or /yoke:fix first.' (or equivalent — verbatim user-facing strings need explicit edits). After the migration, every wm_prd_path call site outside skills/discover/SKILL.md and skills/fix/SKILL.md MUST be removed; this is enforced by the new no-direct-prd-path sensor. Do not branch any skill's core logic on which artifact backed the slug — the artifact is read as opaque Phase-1 input.

**Validation:** (no validation provided)

**Acceptance criterion:** AC-003-1, AC-003-2: grep -rE 'wm_prd_path' skills/ filtered against discover/fix returns zero matches; existing PRD-backed regression suite passes end-to-end through every migrated skill.

### Task 2026-05-05-phase-1-fix-entrypoint-s02-t04

**Story:** Add tests/sensors/no-direct-prd-path.test.sh as a CI gate that grep-audits the skills/ tree for any wm_prd_path read outside skills/discover/SKILL.md and skills/fix/SKILL.md. The sensor follows the structured-sensor-output contract from concepts/yoke-pattern-sensors: success silent, failure prints violation identification (the file path of the offending reader) + location (the matching line) + correction instruction (rewrite to wm_phase1_artifact_path). Wire the sensor into .github/workflows/tests.yml so it runs on every PR. Add a self-test under tests/sensors/no-direct-prd-path.test.sh that exercises both the pass and fail paths against fixture skill bodies. (Realizes: US-003)

**Technical implementation:** Add tests/sensors/no-direct-prd-path.test.sh as a CI gate that grep-audits the skills/ tree for any wm_prd_path read outside skills/discover/SKILL.md and skills/fix/SKILL.md. The sensor follows the structured-sensor-output contract from concepts/yoke-pattern-sensors: success silent, failure prints violation identification (the file path of the offending reader) + location (the matching line) + correction instruction (rewrite to wm_phase1_artifact_path). Wire the sensor into .github/workflows/tests.yml so it runs on every PR. Add a self-test under tests/sensors/no-direct-prd-path.test.sh that exercises both the pass and fail paths against fixture skill bodies.

**Validation:** (no validation provided)

**Acceptance criterion:** AC-003-1: the sensor exits 0 when only discover and fix skills read wm_prd_path; exits non-zero with structured diagnostic when any other skill reads it.

### Task 2026-05-05-phase-1-fix-entrypoint-s02-t05

**Story:** Add tests/sensors/no-ambiguous-phase1.test.sh as a CI gate that scans every test-output stream produced during the test suite for the substring 'wm: ambiguous Phase-1 state'. If found in any main-branch CI run, the sensor fails the build with a structured diagnostic naming the offending pipeline step. The sensor follows the structured-sensor-output contract. Wire into .github/workflows/tests.yml. The substring is the verbatim ratified diagnostic from FR-9 / AC-004-1, providing the stable target the sensor depends on. (Realizes: US-004)

**Technical implementation:** Add tests/sensors/no-ambiguous-phase1.test.sh as a CI gate that scans every test-output stream produced during the test suite for the substring 'wm: ambiguous Phase-1 state'. If found in any main-branch CI run, the sensor fails the build with a structured diagnostic naming the offending pipeline step. The sensor follows the structured-sensor-output contract. Wire into .github/workflows/tests.yml. The substring is the verbatim ratified diagnostic from FR-9 / AC-004-1, providing the stable target the sensor depends on.

**Validation:** (no validation provided)

**Acceptance criterion:** AC-004-4: the no-ambiguous-phase1 sensor fails CI on any test run that emits 'wm: ambiguous Phase-1 state'.

### Task 2026-05-05-phase-1-fix-entrypoint-s02-t06

**Story:** Add three test artifacts: (1) tests/smoke/fix-skill-happy-path.test.sh — exercises /yoke:fix on a fixture problem statement, verifies the fix-spec materializes at .yoke/fixes/<slug>.md with scope_caution: empty for a 4/4-readiness + 4/4-narrowness input, and approval menu option 1 chains into /yoke:tech-spec via Skill. (2) tests/smoke/fix-spec-end-to-end.test.sh — runs the full chain (/yoke:fix → /yoke:tech-spec → /yoke:acceptance-criteria → /yoke:generate-sprints → first cycle of /yoke:implement → /yoke:canonize) on a fix-spec-backed slug and asserts no path-resolution errors at any step. (3) tests/regression/phase1-backward-compat/ — directory with ≥ 5 representative PRD-backed scenario fixtures (one per major Phase-1 use case from the project's history) that walk the full chain post-migration and assert end-to-end success. All three use the existing tests/lib/harness.sh; all three follow the watchdog convention (sleep 600 && kill -TERM $$ &). (Realizes: US-001, US-003)

**Technical implementation:** Add three test artifacts: (1) tests/smoke/fix-skill-happy-path.test.sh — exercises /yoke:fix on a fixture problem statement, verifies the fix-spec materializes at .yoke/fixes/<slug>.md with scope_caution: empty for a 4/4-readiness + 4/4-narrowness input, and approval menu option 1 chains into /yoke:tech-spec via Skill. (2) tests/smoke/fix-spec-end-to-end.test.sh — runs the full chain (/yoke:fix → /yoke:tech-spec → /yoke:acceptance-criteria → /yoke:generate-sprints → first cycle of /yoke:implement → /yoke:canonize) on a fix-spec-backed slug and asserts no path-resolution errors at any step. (3) tests/regression/phase1-backward-compat/ — directory with ≥ 5 representative PRD-backed scenario fixtures (one per major Phase-1 use case from the project's history) that walk the full chain post-migration and assert end-to-end success. All three use the existing tests/lib/harness.sh; all three follow the watchdog convention (sleep 600 && kill -TERM $$ &).

**Validation:** (no validation provided)

**Acceptance criterion:** AC-001-1, AC-003-2, AC-003-3: the happy-path smoke passes for fix-spec invocations; the regression suite passes for ≥ 5 PRD-backed scenarios; the fix-spec-end-to-end smoke completes the full lifecycle without path errors.

## Functional acceptance criteria

- US-001
- US-003
- US-004

## Sensors

- code-review
- lint
- tests-sensors
- tests-smoke

---

> Generated by `/yoke:generate-sprints` (Stage 5) from `.yoke/runtime/.generate-sprints-plan.yaml`. Status flips to `approved` when Trigger 2.5 ratifies the sprint plan.