---
task_id: 2026-05-03-tech-spec-as-design-doc-s02
sprint: 2
slug: 2026-05-03-tech-spec-as-design-doc
status: approved
created_at: 2026-05-03T06:32:13Z
model: ""
traceability: ""
Migrated-from: []
---

# Sprint 02 of 02: Council prompts, smoke tests, dogfood validation

> Generated by `/yoke:tech-spec` (stage 3) from
> `.yoke/specs/2026-05-03-tech-spec-as-design-doc.md`. Status flips to `approved` when Trigger 2
> approves the parent spec.

## Sprint objective

Harden and validate the Phase-2/3 cutover landed in Sprint 01.
`agents/sr-eng.md`, `agents/sr-qa.md`, and `agents/sr-staff.md` are
updated so each council persona explicitly consumes the per-sprint
runtime bundle (`.yoke/sprints/<slug>-s<NN>.md`) as the cycle's
working set, with the spec held as read-only architectural context
and the contract as the binding source of truth. Three new smoke
tests under `tests/smoke/` pin the new Phase-2 design-doc shape, the
new Phase-3 sprint-generation contract, and the relocated
`scaffold-sprints.sh` invocation site so regressions are caught in
CI. One end-to-end dogfood run drives a small follow-up PRD through
`/yoke:discover` → `/yoke:tech-spec` → `/yoke:acceptance-contract`
on the new code paths and confirms artifact-shape integrity by
re-running the new smoke tests against the dogfood artifacts.

## Sprint DoD

- `agents/sr-eng.md`, `agents/sr-qa.md`, and `agents/sr-staff.md` each contain at least one literal reference to `.yoke/sprints/<slug>-s` (the per-cycle working-set path pattern) — `grep -lE '\.yoke/sprints/<slug>-s' agents/sr-eng.md agents/sr-qa.md agents/sr-staff.md | wc -l` returns `3`.
- A council cycle on a synthetic test sprint produces persona slices at `.yoke/runtime/cycles/<N>/<persona>.md` for each of the three personas, and each slice's body cites the sprint file path (not the spec path) at least once.
- `bash tests/smoke/tech-spec-design-doc-shape.test.sh` exists and exits 0 against the post-cutover code base.
- `bash tests/smoke/acceptance-contract-sprint-generation.test.sh` exists and exits 0 against the post-cutover code base.
- `bash tests/smoke/scaffold-sprints-callable-from-phase-3.test.sh` exists and exits 0 against the post-cutover code base.
- Each new smoke test contains an internal watchdog (`sleep 600 && kill -TERM $$ &`) per repo convention — `grep -c 'sleep 600 && kill -TERM' tests/smoke/{tech-spec-design-doc-shape,acceptance-contract-sprint-generation,scaffold-sprints-callable-from-phase-3}.test.sh | awk -F: '{ s+=$2 } END { print s }'` returns `>= 3`.
- A dogfood run on a fresh slug produces `.yoke/prds/<dogfood-slug>.md`, `.yoke/specs/<dogfood-slug>.md`, `.yoke/acceptance-contracts/<dogfood-slug>.md`, and `>= 1` `.yoke/sprints/<dogfood-slug>-s*.md` files; all four artifact types co-exist for the same slug after a single end-to-end run.
- Re-running the three smoke tests against the dogfood slug's artifacts (parameterized via the slug under test) all exit 0.
- `.github/workflows/tests.yml` runs the three new smoke tests on every PR (verified by reading the workflow file and confirming the new tests are matched by the existing test discovery glob `tests/smoke/*.test.sh`).

## Tasks

### Task 2026-05-03-tech-spec-as-design-doc-s02-t01

**Story:** Update the three council personas so each consumes the per-sprint runtime bundle as the cycle's working set, with the spec held as read-only architectural context.

**Technical implementation:** Edit `agents/sr-eng.md`, `agents/sr-qa.md`, and `agents/sr-staff.md`. In each file, add (or update) the working-set declaration so it explicitly reads `.yoke/sprints/<slug>-s<current_sprint>.md` as the per-cycle bundle path (resolved via `current_sprint:` in `.yoke/runtime/progress.md`); cite `concepts/yoke-pattern-sprint-runtime-bundle` (the canonized pattern that pins this shape) in the prompt body. The spec at `.yoke/specs/<slug>.md` becomes a read-only architectural context the persona MAY query for the twelve design-doc sections (architecture, NFRs, alternatives, technical use cases) but MUST NOT iterate on for tasks. The binding `acceptance-contracts/<slug>.md` remains the source of truth (unchanged). Sr Eng's prompt explicitly reads each `### Task <ID>` anchor inside the sprint file and writes its slice referencing the task ID. Sr QA's prompt reads each `### Task <ID>` and writes acceptance-contract-anchored tests under `tests/acceptance/<contract-slug>/` keyed on the task ID. Sr Staff's prompt reads the sprint file's `## Functional acceptance criteria` and `## Sensors` sections to scope the architectural review surface for the cycle. Per-persona slice paths (`.yoke/runtime/cycles/<N>/<persona>.md`) and the sync-barrier marker shape (`.yoke/runtime/.phase-a-done.<persona>`) are unchanged.

**Validation:** `grep -lE '\.yoke/sprints/<slug>-s' agents/sr-eng.md agents/sr-qa.md agents/sr-staff.md | wc -l` returns `3`. `grep -lE 'concepts/yoke-pattern-sprint-runtime-bundle' agents/sr-eng.md agents/sr-qa.md agents/sr-staff.md | wc -l` returns `3`. Run a synthetic council cycle (Phase A spawn) against a tiny test sprint with one task anchor; assert each of `.yoke/runtime/cycles/1/sr-eng.md`, `.yoke/runtime/cycles/1/sr-qa.md`, `.yoke/runtime/cycles/1/sr-staff.md` exists and that each slice cites the sprint file path at least once (`grep -c '\.yoke/sprints/' "$slice"` returns `>= 1` for each). Negative test: a persona slice that cites the spec path INSTEAD of the sprint path is acceptable for read-only context but MUST also cite the sprint path at least once for task work.

**Acceptance criterion:** All three persona files reference `.yoke/sprints/<slug>-s` as the per-cycle working set AND cite `concepts/yoke-pattern-sprint-runtime-bundle` in their prompt body, AND a synthetic council cycle produces three persona slices that each cite the sprint file path at least once (no slice references only the spec path for task iteration).

### Task 2026-05-03-tech-spec-as-design-doc-s02-t02

**Story:** Add three smoke tests that pin the new Phase-2 design-doc shape, the new Phase-3 sprint-generation contract, and the relocated `scaffold-sprints.sh` invocation site so regressions are caught in CI.

**Technical implementation:** Create three test files under `tests/smoke/` following the repo convention (bash 4+; internal watchdog `sleep 600 && kill -TERM $$ &`; `set -euo pipefail`; isolated tempdir per test; cleanup trap). Test 1 — `tests/smoke/tech-spec-design-doc-shape.test.sh`: scaffold a tiny test PRD (≥ 1 US-### + ≥ 1 FR-N) at a tempdir's `.yoke/prds/<slug>.md`; drive it through `/yoke:tech-spec` (via a synthetic skill harness or by directly executing the skill body's bash steps); assert (a) `grep -cE '^## (Context and Scope|Goals and Non-Goals|System Context|Architecture|Stack and Dependencies|APIs and Data Model|Non-Functional Requirements|Alternatives Considered|Trade-offs|Cross-cutting Concerns|Technical Use Cases|Open Questions)$' "$tempdir/.yoke/specs/$slug.md"` returns `12`; (b) headings appear in the documented order; (c) `find $tempdir/.yoke/sprints -name "${slug}-s*.md" | wc -l` returns `0`; (d) `grep -cE '^### Task ' "$tempdir/.yoke/specs/$slug.md"` returns `0`; (e) every NFR bullet matches the quantitative-target regex; (f) every `### Alt:` subsection contains all three required labels; injecting an adjective-only NFR or an alternative missing `**Source:**` causes the skill to abort non-zero. Test 2 — `tests/smoke/acceptance-contract-sprint-generation.test.sh`: continue from test 1's tempdir state; drive the same slug through `/yoke:acceptance-contract`; assert (a) `[ -f "$tempdir/.yoke/acceptance-contracts/$slug.md" ]`; (b) `find $tempdir/.yoke/sprints -name "${slug}-s*.md" | wc -l` returns `>= 1`; (c) every sprint file matches the five required H2 headings in order; (d) every sprint file has `>= 1` `### Task <slug>-s<NN>-t<MM>` subsection with all four inline labels; (e) the contract's BDD scenario count matches the PRD's US-### + FR-N count (with FR-Ns covered by US-IDs deduplicated). Test 3 — `tests/smoke/scaffold-sprints-callable-from-phase-3.test.sh`: write a synthetic sprint-plan markdown buffer with three `### Sprint <NN> — <name>` headings; invoke `bash lib/working-memory/scaffold-sprints.sh "$buffer"`; assert exit 0 and exactly three empty sprint files materialized at `.yoke/sprints/`, each carrying the frontmatter stub + the five-H2 skeleton from `templates/sprint.md`. Each test prints a final `OK <test-name>` line on success or surfaces the failing assertion's path/line on failure. Confirm `.github/workflows/tests.yml` already discovers `tests/smoke/*.test.sh` via its existing glob — no workflow change needed.

**Validation:** `bash tests/smoke/tech-spec-design-doc-shape.test.sh` exits 0 against the post-cutover code base; `bash tests/smoke/acceptance-contract-sprint-generation.test.sh` exits 0; `bash tests/smoke/scaffold-sprints-callable-from-phase-3.test.sh` exits 0. Each test file contains the `sleep 600 && kill -TERM $$ &` watchdog (verified by `grep`). Negative regression: checkout the pre-Sprint-01 code; all three tests exit non-zero (proving they actually pin the new behavior, not generic happy-path checks). CI dry-run: simulate a PR by running `bash tests/run-all.sh` (or equivalent) and confirm the three new tests are picked up by the test discovery glob.

**Acceptance criterion:** All three new smoke test files exist under `tests/smoke/`, each carries the watchdog idiom, and `bash tests/smoke/tech-spec-design-doc-shape.test.sh && bash tests/smoke/acceptance-contract-sprint-generation.test.sh && bash tests/smoke/scaffold-sprints-callable-from-phase-3.test.sh` all exit 0 in a clean checkout of the post-cutover branch. The three tests exit non-zero against the pre-Sprint-01 branch (regression-direction check).

### Task 2026-05-03-tech-spec-as-design-doc-s02-t03

**Story:** Run a single end-to-end dogfood validation that drives a small follow-up PRD through the new flow and confirms artifact-shape integrity by re-running the smoke tests against the dogfood artifacts.

**Technical implementation:** Pick a small follow-up improvement scoped narrowly enough to converge in one pass (e.g., "add a `--dry-run` flag to `/yoke:tech-spec` that prints the dialogue plan without writing files" or "expose `wm_spec_path` in `lib/working-memory/paths.sh` shell completion" — final pick decided at sprint runtime, not in this PRD). Drive it through `/yoke:discover` → Trigger 1 → `/yoke:tech-spec` → Trigger 2 → `/yoke:acceptance-contract` → Trigger 3 against the new code paths only. Capture the produced artifacts (one PRD + one spec + one contract + ≥ 1 sprint file) under `.yoke/{prds,specs,acceptance-contracts,sprints}/` for the dogfood slug. Re-run the three smoke tests from t02, parameterized to assert against the dogfood slug's artifacts (the smoke tests accept a `SLUG_UNDER_TEST` env var that defaults to a synthetic test slug; the dogfood run sets it to the dogfood slug). Capture three confirmation signals: (i) all three smoke tests pass against the dogfood artifacts; (ii) a manual read of the produced spec by the maintainer confirms the twelve sections render coherently and the user-stories lift produces useful technical context (subjective, but a binary "useful / not useful" call); (iii) the produced contract's BDD scenario count equals `count(US-###) + count(FR-N not covered)`.

**Validation:** A complete artifact set (`<dogfood-slug>.md` PRD + spec + contract + ≥ 1 sprint file) exists at `.yoke/{prds,specs,acceptance-contracts,sprints}/`; `SLUG_UNDER_TEST=<dogfood-slug> bash tests/smoke/tech-spec-design-doc-shape.test.sh && SLUG_UNDER_TEST=<dogfood-slug> bash tests/smoke/acceptance-contract-sprint-generation.test.sh` both exit 0; the maintainer's read confirms the twelve sections and the US-### lift produce useful output (recorded as a one-line note in the dogfood slug's contract `## Notes` block). Negative case: if the maintainer's read fails the "useful / not useful" call, the dogfood run is recorded as a Trigger-4-equivalent escalation candidate (the persona-divergence escape hatch) and Sprint 02 does not exit until the issue is reproduced, isolated, and either fixed in code (loops back to t01 / t02) or captured in a fleeting note for canonization at the next `/yoke:canonize` run. The dogfood run does NOT block on aesthetic disagreement — only on shape-integrity violations or a clear "the spec is unreadable / the lift is wrong" verdict.

**Acceptance criterion:** A single dogfood slug exists at `.yoke/{prds,specs,acceptance-contracts,sprints}/<dogfood-slug>.md` (and `<dogfood-slug>-s*.md` for sprints), all four artifact types present for the same slug, AND `SLUG_UNDER_TEST=<dogfood-slug> bash tests/smoke/tech-spec-design-doc-shape.test.sh && SLUG_UNDER_TEST=<dogfood-slug> bash tests/smoke/acceptance-contract-sprint-generation.test.sh` both exit 0, AND the maintainer records a `## Notes` block in the dogfood contract confirming the twelve sections render coherently.

## Functional acceptance criteria

- `council-personas-consume-sprint-files`
- `council-personas-cite-sprint-runtime-bundle-pattern`
- `council-cycle-slices-cite-sprint-path`
- `smoke-tests-tech-spec-design-doc-shape`
- `smoke-tests-acceptance-contract-sprint-generation`
- `smoke-tests-scaffold-sprints-from-phase-3`
- `smoke-tests-watchdog-present`
- `smoke-tests-discoverable-by-ci`
- `dogfood-end-to-end-artifact-set-complete`
- `dogfood-smoke-tests-pass-against-dogfood-slug`
- `dogfood-maintainer-read-confirms-coherence`

## Sensors

- `council-persona-sprint-path-reference`
- `council-persona-pattern-citation`
- `council-cycle-slice-sprint-path-citation`
- `smoke-tests-pass`
- `smoke-tests-watchdog-present`
- `dogfood-artifact-shape-check`
- `dogfood-smoke-test-parameterized-pass`

---

> Generated by `/yoke:tech-spec` (stage 3) from `.yoke/specs/2026-05-03-tech-spec-as-design-doc.md`.
> Status flips to `approved` when Trigger 2 approves the parent spec.
