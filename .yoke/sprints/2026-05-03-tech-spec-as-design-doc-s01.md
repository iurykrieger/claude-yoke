---
task_id: 2026-05-03-tech-spec-as-design-doc-s01
sprint: 1
slug: 2026-05-03-tech-spec-as-design-doc
status: approved
created_at: 2026-05-03T06:32:13Z
model: ""
traceability: ""
Migrated-from: []
---

# Sprint 01 of 02: Phase-2/3 atomic cutover

> Generated by `/yoke:tech-spec` (stage 3) from
> `.yoke/specs/2026-05-03-tech-spec-as-design-doc.md`. Status flips to `approved` when Trigger 2
> approves the parent spec.

## Sprint objective

Ship the atomic Phase-2/3 cutover end-to-end so the framework's
behavior is preserved between sprints and the new design-doc surface
is live in one move. By sprint exit, `/yoke:tech-spec` produces the
twelve-H2-section design doc with stack-negotiation dialogue,
canonical-memory + threshold-gated `WebSearch` sourcing, and post-
draft self-checks, and never writes any sprint file or `### Task <ID>`
anchor; `/yoke:acceptance-contract` runs the new Stage A (BDD draft)
+ Stage B (sprint plan → `scaffold-sprints.sh` → LLM-per-sprint fill)
flow with atomic Trigger-3 covering the contract plus every produced
sprint file. The `templates/sprint.md` shape stays unchanged so
Phase 4 (`/yoke:implement`, council ralph loop) keeps consuming
sprint files transparently.

## Sprint DoD

- `grep -cE '^## (Context and Scope|Goals and Non-Goals|System Context|Architecture|Stack and Dependencies|APIs and Data Model|Non-Functional Requirements|Alternatives Considered|Trade-offs|Cross-cutting Concerns|Technical Use Cases|Open Questions)$' templates/spec.md` returns exactly `12`.
- `head -30 templates/spec.md` shows the twelve H2 headings appearing in the documented order with no extra H2s in between.
- Running `/yoke:tech-spec` against a freshly-approved test PRD produces exactly one file under `.yoke/specs/` and zero files under `.yoke/sprints/` (`find .yoke/sprints -name '<slug>-s*.md' 2>/dev/null | wc -l` returns `0` after Phase 2 only).
- `grep -cE '^### Task ' .yoke/specs/<slug>.md` returns `0` for the same Phase-2-only run.
- `skills/tech-spec/SKILL.md` no longer references `lib/working-memory/scaffold-sprints.sh` (`grep -c 'scaffold-sprints' skills/tech-spec/SKILL.md` returns `0`).
- `skills/acceptance-contract/SKILL.md` references `lib/working-memory/scaffold-sprints.sh` exactly once as part of Stage B (`grep -c 'scaffold-sprints' skills/acceptance-contract/SKILL.md` returns `>= 1`).
- `lib/config-overrides.sh` exists and exposes `yoke_get_override <key> <default>` (verified via `bash -c 'source lib/config-overrides.sh && type yoke_get_override' && echo OK`).
- Running `/yoke:acceptance-contract` after `/yoke:tech-spec` against the same slug produces `.yoke/acceptance-contracts/<slug>.md` AND `wm_list_sprint_paths "<slug>" | wc -l` returns `>= 1`.
- Every sprint file produced in Phase 3 carries the five required H2 headings in order (`grep -cE '^## (Sprint objective|Sprint DoD|Tasks|Functional acceptance criteria|Sensors)$'` returns `5`) and ≥ 1 `### Task <ID>` subsection with all four inline labels (`**Story:**`, `**Technical implementation:**`, `**Validation:**`, `**Acceptance criterion:**`).
- The Trigger-3 approval flips `Status: approved` on the contract AND `status: approved` in the frontmatter of every sprint file in a single bash transaction (no partial state observable across the menu boundary).
- Injecting an adjective-only NFR (`- **Performance:** fast`) into the `## Non-Functional Requirements` section makes `/yoke:tech-spec`'s post-draft self-check abort non-zero with a `wm:`-prefixed message naming the offending bullet.
- Injecting an `## Alternatives Considered` subsection missing the `**Source:**` label makes the self-check abort non-zero with a `wm:`-prefixed message naming the offending alternative name.

## Tasks

### Task 2026-05-03-tech-spec-as-design-doc-s01-t01

**Story:** Replace the legacy sprint-pointer `templates/spec.md` with the twelve-H2-section design-doc template, and ship a `lib/config-overrides.sh` helper that the rewritten skill consumes for the canonical-pattern threshold.

**Technical implementation:** Rewrite `templates/spec.md` end-to-end so the body carries exactly twelve H2 sections in fixed order (Context and Scope; Goals and Non-Goals; System Context; Architecture; Stack and Dependencies; APIs and Data Model; Non-Functional Requirements; Alternatives Considered; Trade-offs; Cross-cutting Concerns; Technical Use Cases; Open Questions). Each section carries placeholder body text demonstrating the expected shape: `## Non-Functional Requirements` shows quantitative-target examples (`- **Latency:** p95 < 200ms`); `## Alternatives Considered` shows the `### Alt: <name>` subsection with the three labelled lines (`**Trade-off vs. chosen:**`, `**Reason for rejection:**`, `**Source:**`); `## Technical Use Cases` shows the `### US-### — <title>` subsection with the four labelled lines (`**Components involved:**`, `**Contracts used:**`, `**NFRs applied:**`, `**Edge cases:**`). Front matter (`> Generated by ...`, `> Status: ...`, `> Approved by:`, `> Approved at:`) shape preserved verbatim from the legacy template. Add a new file `lib/config-overrides.sh` that sources cleanly under `bash -u`, exposes `yoke_get_override <key> <default>` reading dotted keys from `.yoke/config.yaml` (using a small `awk`/`sed` parser scoped to the `overrides:` block — yaml is already otherwise hand-parsed in `lib/yoke-prelude.sh`), and falls back to `<default>` when the key is absent. Cite `concepts/yoke-pattern-plugin-structure` for the `lib/` placement rule.

**Validation:** `grep -cE '^## (Context and Scope|Goals and Non-Goals|System Context|Architecture|Stack and Dependencies|APIs and Data Model|Non-Functional Requirements|Alternatives Considered|Trade-offs|Cross-cutting Concerns|Technical Use Cases|Open Questions)$' templates/spec.md` returns `12`; the twelve headings appear in documented order (verified by extracting H2s with `grep -nE '^## ' templates/spec.md | head -12` and diffing against the canonical order list); the placeholder body for `## Non-Functional Requirements` contains at least one numeric-unit pair; the placeholder body for `## Alternatives Considered` contains the three labels; the placeholder body for `## Technical Use Cases` contains the four labels. `bash -c 'source lib/config-overrides.sh && yoke_get_override overrides.tech_spec.canonical_pattern_threshold 3' && echo OK` returns `3` against an unmodified `.yoke/config.yaml`. Set the override to `5` in `.yoke/config.yaml` and the same command returns `5`. Remove the override; default `3` is restored.

**Acceptance criterion:** `templates/spec.md` carries exactly twelve H2 headings matching the documented order (verified by a deterministic grep), AND `bash -c 'source lib/config-overrides.sh && type yoke_get_override' && echo OK` exits 0 with `OK` printed, AND the override reader returns `3` on an unmodified config and the configured value when the override is set.

### Task 2026-05-03-tech-spec-as-design-doc-s01-t02

**Story:** Rewrite `/yoke:tech-spec` so Phase 2 negotiates stack and architecture, drafts the twelve-section design doc with self-checks, and never produces sprint files or task anchors.

**Technical implementation:** Rewrite `skills/tech-spec/SKILL.md` end-to-end. Pre-flight unchanged (`yoke_require_provider`; `wm_active_slug`; PRD existence + approved). Read approved PRD via `Read`. Stack-detection round: read each of `package.json`, `go.mod`, `Cargo.toml`, `requirements.txt`, `pyproject.toml`, `pom.xml`, `Gemfile` via `Read` (skip absent files); when ≥ 2 manifests detected, list every detected stack as a candidate and force the user to pick (no auto-pick). Render the curated stack menu by feature category (web-api / web-ui / cli / library / data-pipeline) with 3-5 lettered options + `Other:` per FR-1 and FR-2. Architecture round: invoke `/yoke:search-canonical-memory` with a query scoped to the feature's domain lifted from the PRD's Introduction; count pattern-typed entities returned; if `count >= yoke_get_override overrides.tech_spec.canonical_pattern_threshold 3`, skip `WebSearch`; otherwise load `WebSearch` via `ToolSearch` (`select:WebSearch`) and run a single targeted query; surface the top 2-3 industry patterns with explicit trade-offs to the user; capture the chosen alternative. NFR round: prompt for quantitative NFRs (latency, availability, throughput, consistency) and challenge adjective-only answers inline. Use-cases round: walk each PRD `US-###` and prompt for Components / Contracts / NFRs / Edge cases. Draft the spec end-to-end in a single `Write` call to `wm_spec_path "$slug"` matching the new `templates/spec.md` shape and lifting US-IDs verbatim into `## Technical Use Cases`. Self-check post-draft: (a) twelve H2 headings present in documented order via `grep -E`; (b) every `## Non-Functional Requirements` bullet matches `\d+(\.\d+)?\s*(ms|s|%|rps|qps|req/s|MB|GB|TB|users)` (case-insensitive); (c) every `### Alt: <name>` subsection carries the three labels (`**Trade-off vs. chosen:**`, `**Reason for rejection:**`, `**Source:**`); (d) every PRD `US-###` appears as a `### US-### — <title>` subsection (or marked `Deferred:` in `## Open Questions`); (e) `grep -cE '^### Task ' "$spec"` returns `0`; (f) `find .yoke/sprints -name "${slug}-s*.md" | wc -l` returns `0`. Any failure aborts the skill non-zero with a `wm:`-prefixed message naming the offending location. Trigger 2: render the shared approval menu via `templates/approval-menu.md`. The Tech-Spec-only summary block (template field `task_summary`) becomes a list of the twelve H2 sections present in the spec (one-line summary per section) — this preserves the per-block contract while reflecting the new shape. Drop the legacy stage-2 invocation of `lib/working-memory/scaffold-sprints.sh` and the legacy stage-3 LLM-per-sprint fill loop. Cite `concepts/yoke-pattern-phase-flow` (Phase 2 still produces tech-spec.md as the binding artifact) and `concepts/yoke-pattern-roles` (Generator persona inline) in the rewritten skill body.

**Validation:** Drive a fresh test PRD (a small ~30-line PRD with US-001 + US-002 + FR-1) through `/yoke:tech-spec` against an isolated working directory. Assert (i) the produced spec at `wm_spec_path "$slug"` contains the twelve H2 sections in order (`grep -cE '^## (Context and Scope|...)$' "$spec"` returns `12`); (ii) `find .yoke/sprints -name "${slug}-s*.md" | wc -l` returns `0`; (iii) `grep -cE '^### Task ' "$spec"` returns `0`; (iv) every PRD `US-###` appears as `### US-### —` in the spec body; (v) every NFR bullet matches the quantitative-target regex; (vi) every `### Alt:` subsection carries all three labels. Negative tests: run the skill against a PRD where the dialogue answers include an adjective-only NFR — the post-draft self-check aborts non-zero with a `wm:` message naming the offending bullet. Run the skill against a PRD where the dialogue answers omit `**Source:**` from an alternative — self-check aborts non-zero. Threshold test: set `overrides.tech_spec.canonical_pattern_threshold: 99` in `.yoke/config.yaml`; assert `WebSearch` fires (the skill prints the WebSearch dispatch line and the spec's `## Alternatives Considered` cites at least one URL returned by the search). Reset the override; assert WebSearch is NOT fired (no WebSearch line printed; `## Alternatives Considered` cites canonical-memory wikilinks only).

**Acceptance criterion:** Running `/yoke:tech-spec` against an approved test PRD produces exactly one file at `wm_spec_path "$slug"` whose body satisfies all five greps simultaneously: (1) `grep -cE '^## (Context and Scope|Goals and Non-Goals|System Context|Architecture|Stack and Dependencies|APIs and Data Model|Non-Functional Requirements|Alternatives Considered|Trade-offs|Cross-cutting Concerns|Technical Use Cases|Open Questions)$' "$spec"` returns `12`; (2) `grep -cE '^### Task ' "$spec"` returns `0`; (3) `find .yoke/sprints -name "${slug}-s*.md" | wc -l` returns `0`; (4) every NFR bullet matches the quantitative-target regex; (5) every `### Alt: <name>` subsection contains all three required labels. Adjective-only NFRs and Alternatives missing `**Source:**` cause the skill to abort non-zero with a `wm:` message.

### Task 2026-05-03-tech-spec-as-design-doc-s01-t03

**Story:** Rewrite `/yoke:acceptance-contract` to absorb sprint partition + task breakdown via Stage A (BDD draft) + Stage B (sprint plan → scaffold → LLM-per-sprint fill), with atomic Trigger-3 covering the contract plus every produced sprint file.

**Technical implementation:** Rewrite `skills/acceptance-contract/SKILL.md` to follow the two-stage flow. Pre-flight unchanged (provider hard-break; PRD + spec approved; `wm_active_slug`). Drop the existing pre-flight check that requires sprint files to exist before Phase 3 (`wm_list_sprint_paths "$slug"` zero-paths abort) — that check is incompatible with the new ownership; sprint files are now Phase 3's output. Stage A (LLM): read approved PRD + approved spec; draft `.yoke/acceptance-contracts/<slug>.md` end-to-end with one BDD scenario per PRD `US-###` (mapped through the spec's `### US-### — <title>` subsections in `## Technical Use Cases`) plus one BDD scenario per `FR-N` not covered by a US-### scenario. Use the existing `templates/acceptance-contract.md` shape (binding statement; scenarios block; sensors block; fixtures block) — shape preserved. Stage B.1 (LLM): inside the contract body, draft a new `## Sprint plan` section carrying ordered `### Sprint <NN> — <name>` headings with delivery objectives plus an ordered task list per sprint that references US-### IDs and FR-N IDs back to the BDD scenarios. The heading shape exactly matches what `lib/working-memory/scaffold-sprints.sh` already parses. Stage B.2 (bash): invoke `bash lib/working-memory/scaffold-sprints.sh "$(wm_acceptance_contract_path "$slug")"` against the contract file (the script's argument is generalized: it parses `### Sprint <NN> — <name>` from any markdown file). The script materializes one empty `.yoke/sprints/<slug>-s<NN>.md` per heading seeded from `templates/sprint.md`. Stage B.3 (LLM-per-sprint): for each empty sprint file in lexical order via `wm_list_sprint_paths "$slug"`, lift the sprint preamble (sprint name + delivery objective + task list with criterion-ID and sensor-ID hints) from the contract's `## Sprint plan` section, read the empty sprint file, fill the five required H2 sections (`## Sprint objective`, `## Sprint DoD`, `## Tasks` with `### Task <slug>-s<NN>-t<MM>` subsections carrying the four inline labels, `## Functional acceptance criteria` with criterion IDs only, `## Sensors` with sensor IDs only) per `templates/sprint.md`. Each per-sprint call carries only the sprint preamble and the single empty sprint file — no siblings, no full contract, per the progressive-disclosure rule. Validate post-write that each sprint file matches the five required H2 headings and that every `### Task <ID>` subsection carries all four labels; partial failure exits non-zero with a `wm:`-prefixed message naming the failing file. Trigger 3 (atomic): render the shared approval menu with the binding statement preserved verbatim (the contract IS the binding artifact). On `approve` / `approve_and_continue`, write `Status: approved` to the contract AND iterate `wm_list_sprint_paths "$slug"` setting `status: approved` in each sprint file's frontmatter — all in a single bash transaction. Filesystem failure on any single file aborts the whole transaction non-zero before chaining into `/yoke:implement`. Cite `concepts/yoke-pattern-sprint-runtime-bundle` (sprint files preserved shape; only producer changes) and `concepts/yoke-conventions` (blueprints wrapping agentic nodes — the 3-sub-stage Stage B is the new blueprint).

**Validation:** Drive the same test PRD from t02 through `/yoke:acceptance-contract` after `/yoke:tech-spec`. Assert (i) `.yoke/acceptance-contracts/${slug}.md` exists with `Status: draft` (or `approved` after Trigger 3); (ii) `wm_list_sprint_paths "$slug" | wc -l` returns `>= 1`; (iii) every sprint file matches `grep -cE '^## (Sprint objective|Sprint DoD|Tasks|Functional acceptance criteria|Sensors)$'` returning `5`; (iv) every sprint file has `>= 1` `### Task <slug>-s<NN>-t<MM>` subsection containing all four labels (`**Story:**`, `**Technical implementation:**`, `**Validation:**`, `**Acceptance criterion:**`); (v) the contract carries one BDD scenario per US-### plus one per FR-N not already covered. Atomic-approval test: simulate a filesystem write failure on the second sprint file's `status:` flip during Trigger 3; assert the skill aborts non-zero before chaining into `/yoke:implement` and that the contract's `Status:` is not flipped to `approved` either (no partial state). Partial-state-aborts test: between Stage B.2 and B.3, manually delete one of the scaffolded empty sprint files; the skill aborts non-zero in Stage B.3 with a `wm:` message naming the missing file. Pre-flight regression test: against a slug where Phase 2 produced no sprint files (the new normal), the skill MUST proceed (the legacy "sprint files missing → abort" guard is removed). Sprint-file consistency test: after a successful run, every sprint file's `task_id`, `sprint`, `slug`, `status: draft` (or `approved`) frontmatter matches its filename.

**Acceptance criterion:** Running `/yoke:acceptance-contract` after `/yoke:tech-spec` for the same approved slug produces `.yoke/acceptance-contracts/<slug>.md` AND `>= 1` file under `.yoke/sprints/<slug>-s*.md`, where every sprint file passes `grep -cE '^## (Sprint objective|Sprint DoD|Tasks|Functional acceptance criteria|Sensors)$'` returning exactly `5` AND contains `>= 1` `### Task <ID>` anchor with all four inline labels present, AND the Trigger-3 approval is atomic (any single-file write failure rolls back to no-status-flipped state and the skill exits non-zero before chaining).

## Functional acceptance criteria

- `tech-spec-template-12-section-shape`
- `tech-spec-no-task-anchors-emitted`
- `tech-spec-no-sprint-files-written`
- `tech-spec-stack-auto-detect`
- `tech-spec-stack-menu-curated`
- `tech-spec-canonical-memory-query`
- `tech-spec-web-search-threshold-fallback`
- `tech-spec-quantitative-nfr-self-check`
- `tech-spec-alternatives-triple-self-check`
- `tech-spec-us-id-lifted-into-technical-use-cases`
- `acceptance-contract-stage-a-bdd-per-us`
- `acceptance-contract-stage-b-sprint-plan-headings`
- `acceptance-contract-stage-b-scaffold-sprints-invoked`
- `acceptance-contract-stage-b-per-sprint-fill-progressive-disclosure`
- `acceptance-contract-trigger-3-atomic-approval`
- `acceptance-contract-partial-state-aborts`
- `acceptance-contract-no-prerequisite-sprint-files`
- `scaffold-sprints-callable-from-phase-3`
- `config-override-canonical-pattern-threshold`

## Sensors

- `tech-spec-design-doc-shape`
- `tech-spec-no-task-anchors`
- `tech-spec-no-sprint-files-from-phase-2`
- `tech-spec-quantitative-nfr-rejector`
- `tech-spec-alternatives-triple-rejector`
- `acceptance-contract-sprint-files-produced`
- `acceptance-contract-sprint-file-five-h2-shape`
- `acceptance-contract-task-anchor-four-labels`
- `acceptance-contract-trigger-3-atomic-write`
- `scaffold-sprints-callable-from-phase-3`
- `config-override-reader-callable`

---

> Generated by `/yoke:tech-spec` (stage 3) from `.yoke/specs/2026-05-03-tech-spec-as-design-doc.md`.
> Status flips to `approved` when Trigger 2 approves the parent spec.
