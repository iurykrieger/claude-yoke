---
task_id: 2026-05-03-acceptance-criteria-refactor-s05
sprint: 5
slug: 2026-05-03-acceptance-criteria-refactor
status: approved
created_at: 2026-05-03T06:44:00Z
model: ""
traceability: ""
Migrated-from: []
---

# Sprint 05 of 05: Docs, CLAUDE.md, doctrine cutover

> Generated by `/yoke:tech-spec` (stage 3) from
> `.yoke/specs/2026-05-03-acceptance-criteria-refactor.md`. Status flips to `approved` when Trigger 2
> approves the parent spec.

## Sprint objective

Update every documentation surface to reflect the v4.0.0 cutover. The
project `CLAUDE.md` is updated end-to-end (Where things live, Six-phase
flow, Non-negotiable invariants, Sprint discipline, Migration history).
The host-project template `templates/project-claude-md.md` is updated
to mirror those changes. `docs/architecture.md` reflects the new
artifact name and the DoD/AC split. `docs/migration-v3-to-v4.md`
documents the cutover decision and the explicit no-historical-migration
policy. A final orphan-ref smoke sensor asserts zero
`acceptance-contract` references in active code paths outside
`.yoke/acceptance-contracts/historical/` and an explicit allowlist of
intentional historical references in `docs/migration-v3-to-v4.md` and
the "Migration history" block of `CLAUDE.md`. After this sprint the
v4.0.0 cutover is complete and ready for a single canonization PR.

## Sprint DoD

- `grep -E 'acceptance-criteria' CLAUDE.md | wc -l` returns at least 5.
- `grep -E 'acceptance-contracts?' CLAUDE.md` returns matches only inside the "Migration history" block (allowlist verified by the orphan-ref test).
- `grep -E 'acceptance-criteria' templates/project-claude-md.md | wc -l` returns at least 3.
- `test -f docs/migration-v3-to-v4.md`.
- `grep -E 'acceptance-criteria' docs/architecture.md | wc -l` returns at least 3.
- `bash tests/unit/claude-md-shape.test.sh` exits 0.
- `bash tests/unit/host-claude-md-template-shape.test.sh` exits 0.
- `bash tests/unit/architecture-md-shape.test.sh` exits 0.
- `bash tests/unit/migration-doc-shape.test.sh` exits 0.
- `bash tests/smoke/orphan-acceptance-contract-refs.test.sh` exits 0.

## Tasks

### Task 2026-05-03-acceptance-criteria-refactor-s05-t01

**Story:** Update the project-level `CLAUDE.md` end-to-end so contributors read the v4.0.0 artifact shape and the DoD-vs-AC distinction directly from the agent runtime guidance.

**Technical implementation:** Edit `CLAUDE.md` at the repo root. Updates: (a) "Where things live" block — replace `.yoke/acceptance-contracts/<slug>.md` with `.yoke/acceptance-criteria/<slug>.md`; add a one-line note distinguishing DoD (binary completion checklist) from AC (multi-criterion observable quality conditions); (b) "Six-phase flow" — Phase 3 line renamed to "Acceptance Criteria → approved `acceptance-criteria.md` (binding)"; (c) "Non-negotiable invariants > Binding spec" — keep semantics, rename "Acceptance Contract" to "Acceptance Criteria document" while preserving "operationally defines what 'done' means"; (d) "Sprint discipline" bullet that names `/yoke:acceptance-contract` → `/yoke:acceptance-criteria`; (e) "Migration history" — append a new dated entry "**2026-05-03 (v4.0.0)** — **Phase 3 acceptance-criteria cutover.** …" citing the PRD/Spec/Sprint paths and the explicit no-historical-migration policy. The "Migration history" block becomes the only place inside `CLAUDE.md` where `acceptance-contract(s)` appears (whitelisted by the orphan-ref test).

**Validation:** Add `tests/unit/claude-md-shape.test.sh` asserting: (a) at least 5 matches for `acceptance-criteria` in `CLAUDE.md`; (b) zero matches for `\.yoke/acceptance-contracts/` outside the lines belonging to the "Migration history" block (block delimited by `^## Migration history$` and the next `^## ` heading); (c) the v4.0.0 entry exists with date `2026-05-03`; (d) Phase 3 in the "Six-phase flow" block names `acceptance-criteria.md`.

**Acceptance criterion:** `bash tests/unit/claude-md-shape.test.sh` exits 0.

### Task 2026-05-03-acceptance-criteria-refactor-s05-t02

**Story:** Update `templates/project-claude-md.md` so host projects bootstrapped after v4.0.0 receive a CLAUDE.md that already references the new artifact name and the DoD/AC discipline.

**Technical implementation:** Edit `templates/project-claude-md.md` mirroring the project `CLAUDE.md` updates from s05-t01: artifact references → `acceptance-criteria`; Phase 3 description → `acceptance-criteria.md`; brief DoD-vs-AC note in the "Where things live" or "Sprint discipline" block. Preserve all other content. Do NOT add a Migration history block to the host template — that belongs to this repo only.

**Validation:** Add `tests/unit/host-claude-md-template-shape.test.sh` asserting: (a) at least 3 matches for `acceptance-criteria`; (b) zero matches for `acceptance-contract`; (c) the `/yoke:acceptance-criteria` skill verb appears at least once.

**Acceptance criterion:** `bash tests/unit/host-claude-md-template-shape.test.sh` exits 0.

### Task 2026-05-03-acceptance-criteria-refactor-s05-t03

**Story:** Update `docs/architecture.md` so the architectural overview reflects the new Phase 3 artifact (User Stories → DoD → AC → Sensor pool, runtime sensor selection by council personas).

**Technical implementation:** Edit `docs/architecture.md`. Update: (a) the Phase 3 section's prose to describe the new artifact shape (User Stories at the top level, per-US DoD/AC, document-level Sensor pool); (b) the artifact-flow diagram description (or its prose equivalent) to name `.yoke/acceptance-criteria/`; (c) the sensor-binding paragraph — sensor pool is authoring-time, runtime selection by Sr QA and Sr Staff is documented; (d) any references to `acceptance-contract(s)` outside intentional historical text are replaced or rewritten.

**Validation:** Add `tests/unit/architecture-md-shape.test.sh` asserting: (a) at least 3 matches for `acceptance-criteria`; (b) the document mentions the literal string "Definition of Done" and "Acceptance Criteria"; (c) the sensor-pool runtime-selection narrative is present (regex `runtime selection|select.*pool`); (d) zero matches for `acceptance-contracts/` outside intentional historical references.

**Acceptance criterion:** `bash tests/unit/architecture-md-shape.test.sh` exits 0.

### Task 2026-05-03-acceptance-criteria-refactor-s05-t04

**Story:** Create `docs/migration-v3-to-v4.md` so a host project pinned on `claude-yoke@3.x` has a single doc explaining the breaking change and the upgrade path.

**Technical implementation:** Create `docs/migration-v3-to-v4.md`. Required sections: (a) `## What changed` — one-paragraph summary citing the PRD slug; (b) `## Renames` — table of old→new for the directory (`.yoke/acceptance-contracts/` → `.yoke/acceptance-criteria/`), the skill verb (`/yoke:acceptance-contract` → `/yoke:acceptance-criteria`), the template (`templates/acceptance-contract.md` → `templates/acceptance-criteria.md`), and the path helper (`wm_acceptance_contract_path` → `wm_acceptance_criteria_path`); (c) `## New artifact shape` — minimal template snippet showing US/DoD/AC/Sensor-pool hierarchy; (d) `## Sensor pool — no classification at authoring time` — short paragraph documenting that mandatory/complementary classification is a runtime council decision; (e) `## Behavioral changes` — interactive grill, PRD+Spec resume, lettered options, Quick-Round; (f) `## What is NOT migrated` — explicit "the 17 files under `.yoke/acceptance-contracts/` remain frozen" policy; (g) `## Upgrade steps` — numbered checklist for host projects (re-bootstrap, audit any custom skill that referenced the legacy verb, etc.).

**Validation:** Add `tests/unit/migration-doc-shape.test.sh` asserting: (a) `test -f docs/migration-v3-to-v4.md`; (b) the seven required H2 headings exist verbatim; (c) the rename table contains all four documented rename rows.

**Acceptance criterion:** `bash tests/unit/migration-doc-shape.test.sh` exits 0.

### Task 2026-05-03-acceptance-criteria-refactor-s05-t05

**Story:** Add a final orphan-ref smoke sensor that asserts zero `acceptance-contract` references in active code paths outside the explicit allowlist so no stale reference slips through the cutover.

**Technical implementation:** Add `tests/smoke/orphan-acceptance-contract-refs.test.sh` (`#!/usr/bin/env bash`). The script greps recursively over `skills/`, `agents/`, `lib/`, `hooks/`, `templates/`, `docs/`, `CLAUDE.md`, `.claude-plugin/` for the case-insensitive regex `acceptance-contract(s)?`. The allowlist of intentional historical references: (a) any line inside `docs/migration-v3-to-v4.md`; (b) any line inside the `## Migration history` block of `CLAUDE.md` (delimited by `^## Migration history$` and the next `^## ` heading); (c) any line that is a quoted historical reference inside `Lineage.` blocks of skills (whitelist the literal `Acceptance Contract` mentions inside `> **Lineage.**` quote blocks). Exit non-zero with the offending matches if any unaccounted reference exists. Internal watchdog per `CLAUDE.md` testing convention is not required (script terminates quickly).

**Validation:** Run `bash tests/smoke/orphan-acceptance-contract-refs.test.sh` after every previous sprint task in this PRD has merged. Manually inspect output if the test fails — the failure is the validator. The script's own self-test (a fixture path with a deliberate reference outside the allowlist) lives at `tests/smoke/fixtures/orphan-ref-positive/` and is asserted with `! bash tests/smoke/orphan-acceptance-contract-refs.test.sh --fixture <path>`.

**Acceptance criterion:** `bash tests/smoke/orphan-acceptance-contract-refs.test.sh` exits 0 against the live repo.

## Functional acceptance criteria

- `claude-md-references-acceptance-criteria`
- `host-claude-md-template-updated`
- `architecture-md-reflects-new-artifact`
- `migration-v3-to-v4-doc-exists`
- `orphan-acceptance-contract-refs-zero`

## Sensors

- `lint`
- `tests-smoke`

---

> Generated by `/yoke:tech-spec` (stage 3) from `.yoke/specs/2026-05-03-acceptance-criteria-refactor.md`.
> Status flips to `approved` when Trigger 2 approves the parent spec.
