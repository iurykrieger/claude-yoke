---
task_id: 2026-05-01-agent-council-s04
sprint: 4
slug: 2026-05-01-agent-council
status: approved
created_at: 2026-05-01T11:35:35Z
model: ""
traceability: ""
Migrated-from: []
---

# Sprint 04 of 04: Cutover cleanup, docs, dogfood entry

> Generated by `/yoke:tech-spec` (stage 3) from
> `.yoke/specs/2026-05-01-agent-council.md`. Status flips to `approved` when Trigger 2
> approves the parent spec.
>
> Sprint files are runtime bundles, not sources of truth. Sources of
> truth (Acceptance Contract criteria, sensor logic) stay authoritative
> in their own files; this sprint file references them by ID and never
> inlines their bodies.

## Sprint objective

Remove the v2.x runtime's residual artifacts, update documentation to
reflect the council architecture, and identify the first dogfood task
that will run on the council post-launch (entry point for the v3.0
dogfood follow-up PRD). At sprint end, the framework runs exclusively
on the council runtime: legacy agent files for Generator and Validator
are deleted; the Orchestrator's `monitor` and `consult` modes are
removed from `agents/orchestrator.md` while the `canonize` mode
survives intact for Phase 5; `CLAUDE.md` and `docs/` reflect the
council architecture; a one-line migration note replaces the prior
runbook pattern; and a single `## Dogfood entry point` section in
this sprint's progress committal names the first real Yoke task to
run on v3.0.

## Sprint DoD

- `agents/generator.md` and `agents/validator.md` no longer exist on disk.
- `agents/orchestrator.md` exists but its body covers only the `canonize` mode (the `monitor` and `consult` mode descriptions are removed).
- `lib/runtime/` no longer contains code paths that reference Generator/Validator/Orchestrator-monitor by name (grep -L returns 0 hits across `lib/runtime/`).
- `tests/sensors/legacy-agents-removed.test.sh` exits 0.
- `tests/sensors/orchestrator-canonize-survives.test.sh` exits 0.
- `CLAUDE.md` carries an updated section describing the council runtime (replaces the existing v2.x role description).
- `docs/architecture.md` (existing v2.0.0 dispatch-path doc) is extended with a new diagram or section describing the council protocol.
- A new `docs/migration-v2-to-v3.md` exists with exactly the one-line migration note ("drain in-flight v2.x cycles before upgrading") plus a paragraph context, no formal runbook.
- The sprint's progress committal contains a `## Dogfood entry point` section naming a concrete first Yoke task slug.
- `bumping plugin.json version to 3.0.0` ships in this sprint.

## Tasks

### Task 2026-05-01-agent-council-s04-t01

**Story:** as the framework after the council cutover lands, I have no residual v2.x runtime code paths or agent files; only the council runtime + `Orchestrator-canonize` survive.

**Technical implementation:** delete `agents/generator.md` and `agents/validator.md` (the council personas in `agents/sr-eng.md`, `agents/sr-qa.md`, `agents/sr-staff.md` replace them). Edit `agents/orchestrator.md`: remove the `monitor` and `consult` mode descriptions from the `description` frontmatter field and from the body; preserve the `canonize` mode text intact. Audit `lib/runtime/` and remove every code path that references `generator`, `validator`, or `orchestrator-monitor` / `orchestrator-consult` by name (the council-monitor logic is now in `lib/runtime/council.sh` from Sprint 2 t02; canonize is invoked via `/yoke:canonize` from `skills/canonize/` and stays unchanged). Update `skills/implement/SKILL.md` to remove any remaining references to Generator/Validator. Bump `plugin.json :: version` to `3.0.0` and update the description string to mention "agent council".

**Validation:** `tests/sensors/legacy-agents-removed.test.sh` asserts `agents/generator.md` and `agents/validator.md` do not exist; the orchestrator file's body has no `monitor` or `consult` mode subsections. `tests/sensors/orchestrator-canonize-survives.test.sh` asserts the orchestrator's frontmatter still mentions `canonize` and `/yoke:canonize` is callable end-to-end via a fixture run. Smoke: `grep -lr "generator\|validator\|orchestrator-monitor\|orchestrator-consult" lib/runtime/ skills/implement/ | wc -l` returns 0.

**Acceptance criterion:** `tests/sensors/legacy-agents-removed.test.sh` exits 0 AND `tests/sensors/orchestrator-canonize-survives.test.sh` exits 0 AND `jq -r .version .claude-plugin/plugin.json` returns `3.0.0`.

### Task 2026-05-01-agent-council-s04-t02

**Story:** as a Yoke user reading the docs after v3.0 ships, I find a clear description of the council architecture, a one-line migration note for upgrading from v2.x, and an updated `CLAUDE.md` reflecting the new runtime.

**Technical implementation:** rewrite the `## What Yoke is` and `## Architecture` sections in `CLAUDE.md` to describe the council (three personas, Phase A/B/C, sync barrier, contradiction-detection arbiter, Trigger 4 generalization, Orchestrator-canonize survives). Update the Migration history section with a `**2026-05-XX (v3.0.0)** — Council cutover` entry. Extend `docs/architecture.md` with a new section "Council protocol" containing a diagram (text-based or mermaid) of the cycle phases plus the persona/arbiter dispatch path. Create `docs/migration-v2-to-v3.md` with exactly: the one-line migration note ("drain in-flight v2.x cycles before upgrading"), a short context paragraph explaining why no formal runbook is required (PRD Resolved 10), and a pointer to the council architecture docs. Make sure none of the existing v1→v2 docs are deleted.

**Validation:** `tests/sensors/claude-md-mentions-council.test.sh` greps the file for the strings `agent council`, `Sr Eng`, `Sr QA`, `Sr Staff`, and `Phase A`. `tests/sensors/migration-note-present.test.sh` asserts `docs/migration-v2-to-v3.md` exists and contains the literal one-line migration note. Smoke: a manual review of `docs/architecture.md` confirms the council protocol section is present and references the agent files by name.

**Acceptance criterion:** `tests/sensors/claude-md-mentions-council.test.sh` exits 0 AND `tests/sensors/migration-note-present.test.sh` exits 0 AND `grep -c "Council protocol" docs/architecture.md` returns at least 1.

### Task 2026-05-01-agent-council-s04-t03

**Story:** as the framework about to begin v3.0 dogfood, I commit the first real Yoke task slug that will exercise the council runtime, providing a concrete entry point for the v3.0-dogfood follow-up PRD.

**Technical implementation:** write a `## Dogfood entry point` section to the sprint's progress committal in `.yoke/runtime/progress.md` (or commit it under `.yoke/specs/2026-05-01-agent-council.md` if progress.md is gitignored — the spec is the durable archive). The section names: a concrete Yoke task slug for the first dogfood task (e.g., a small, low-risk improvement like `agents-have-current-sprint` sensor extension or `wm_*` helper polish); the rationale for picking that task (low blast radius, clear acceptance criterion, exercises all three personas); a date target (no specific deadline, just an ordering signal); and the explicit handoff to the v3.0-dogfood follow-up PRD.

**Validation:** smoke test at `tests/runtime/dogfood-entry-shape.test.sh` parses the spec for a `## Dogfood entry point` heading and asserts presence of: a slug-shaped value, a rationale paragraph, and a handoff line. Manual: cross-check the chosen task is small enough to fit one sprint and exercises Sr Eng + Sr QA + Sr Staff without sensor-harness gaps.

**Acceptance criterion:** `tests/runtime/dogfood-entry-shape.test.sh` exits 0 AND `grep -c '^## Dogfood entry point$' .yoke/specs/2026-05-01-agent-council.md` returns exactly 1.

## Functional acceptance criteria

- `legacy-runtime-removed`
- `orchestrator-canonize-intact`
- `docs-reflect-council`
- `migration-note-shipped`
- `dogfood-entry-committed`

## Sensors

- `legacy-generator-removed`
- `legacy-validator-removed`
- `legacy-orchestrator-monitor-removed`
- `legacy-orchestrator-consult-removed`
- `orchestrator-canonize-survives`
- `claude-md-mentions-council`
- `migration-note-present`
- `dogfood-entry-point-identified`

---

> Generated by `/yoke:tech-spec` (stage 3) from `.yoke/specs/2026-05-01-agent-council.md`.
> Status flips to `approved` when Trigger 2 approves the parent spec.
